# UAAD Sprint 6 最终总结、反思与课程建议

> **核心目标：** 对项目多轮迭代后的整体成果、技术实践、团队协作、智能体使用经验、课程建议与配套材料进行完整总结，形成便于评审、复现和后续维护的最终交付说明。
>
> **阶段定位：** Sprint 6 不再扩大业务范围，重点进行总结、缺陷复盘、个人反思、课程反馈、实验材料归档和验收材料结构化。
>
> 最后更新：2026-06-10

---

## 一、项目整体成果概览

UAAD 是一个面向活动发布、活动报名、订单支付、通知触达、个性化推荐与并发抢票场景的全栈系统。经过多轮 Sprint 迭代后，项目已经从基础需求分析、架构设计、前后端功能开发，推进到并发能力验证、安全测试、系统监控、实机演示和最终验收准备阶段。

最终系统形成了较完整的业务闭环：

1. 商户创建活动，活动可经历草稿、预热、发布、下架等状态。
2. 用户浏览活动列表、活动详情、热门活动和推荐活动。
3. 用户对已发布活动发起报名，系统通过 Redis 库存扣减和 Kafka 异步处理保证高并发下的库存一致性。
4. 报名成功后生成订单，用户可以支付订单或在允许状态下取消报名。
5. 系统通过通知模块向用户反馈报名成功、报名失败、订单过期、活动提醒等事件。
6. 推荐模块根据热度、用户行为和协同过滤结果生成活动推荐。
7. Prometheus 与 Grafana 提供系统运行指标观测，JMeter 与 Go benchmark 支持压力测试和抗压能力展示。
8. Sprint 5 补充安全测试、模糊测试和实机演示任务，为最终验收提供风险闭环。

---

## 二、已实现的功能

### 2.1 用户与认证模块

已实现功能：

- 用户注册。
- 用户登录。
- JWT 鉴权。
- 角色区分：普通用户与商户。
- 前端登录态保存、请求拦截、401 处理和退出登录。
- 后端受保护接口统一鉴权。
- 商户接口通过角色权限控制限制普通用户访问。

运行效果：

- 未登录访问受保护接口返回 401。
- 普通用户访问商户创建活动、发布活动、预热活动等接口返回 403。
- token 过期、格式错误、签名错误时会被拒绝。

### 2.2 活动管理模块

已实现功能：

- 活动创建。
- 活动列表查询。
- 活动详情查询。
- 商户活动列表。
- 活动编辑。
- 活动发布。
- 活动预热。
- 活动下架与过期活动处理。
- 活动分类、地点、价格、容量、开始时间、结束时间等基础字段管理。

活动状态：

- `DRAFT`：草稿状态。
- `PREHEAT`：预热状态，可用于展示或营销预热，但尚未正式开放报名。
- `PUBLISHED`：发布状态，用户可以报名。
- `OFFLINE`：下架或过期状态，不再作为可报名活动展示。

运行效果：

- 商户可以先创建草稿活动，再将活动设为预热。
- 预热活动可以继续发布为正式活动。
- 发布时系统会将库存写入 Redis，形成抢票链路所需的库存缓存。
- 过期活动可被自动下架逻辑处理。

### 2.3 报名与抢票模块

已实现功能：

- 用户报名活动。
- Redis 原子扣减库存。
- 重复报名幂等控制。
- 库存不足处理。
- Kafka 异步消息投递。
- Worker 消费报名消息并落库。
- 报名状态查询。
- 报名取消。
- 报名成功后生成订单。
- 报名失败后回滚或记录失败原因。

运行效果：

- 高并发报名下不会出现库存为负或明显超卖。
- 同一用户重复报名同一活动会被识别，避免重复扣减。
- Go 并发测试、JMeter 压测和 Grafana 监控可以共同验证该链路。

### 2.4 订单模块

已实现功能：

- 报名成功后生成订单。
- 订单列表查询。
- 订单详情查询。
- 订单支付。
- 订单状态流转。
- 订单过期扫描。
- 订单过期后通知用户并回滚库存。

运行效果：

- 用户只能查看和支付自己的订单。
- 支付成功后订单状态更新。
- 过期订单会被后台逻辑处理，避免长期占用库存。

### 2.5 通知模块

已实现功能：

- 报名成功通知。
- 报名失败通知。
- 订单过期通知。
- 活动提醒通知。
- 通知列表查询。
- 未读通知数量查询。
- 标记通知已读。

运行效果：

- 用户在报名、支付、过期等关键流程后能够收到业务反馈。
- 用户不能读取或修改其他用户的通知。
- 未读数量能支持前端角标或通知入口展示。

### 2.6 推荐模块

已实现功能：

- 匿名用户热门推荐。
- 登录用户冷启动推荐。
- 用户行为足够时的协同过滤推荐。
- 热门活动接口。
- 用户行为上报。
- 活动热度分数计算。
- 推荐结果缓存与刷新。

推荐策略：

- 匿名用户或无行为用户：使用热门排序。
- 行为数量较少的用户：使用偏好分类补足热门活动。
- 行为数量较多的用户：使用基于物品的协同过滤，并结合热门和新鲜活动兜底。

运行效果：

- 首页或推荐页可以展示热门活动与个性化推荐活动。
- 浏览行为会影响活动热度和后续推荐结果。

### 2.7 前端界面与交互

已实现功能：

- 首页与活动列表。
- 活动详情页。
- 登录与注册。
- 商户活动管理。
- 活动创建、编辑、发布、预热入口。
- 用户报名流程。
- 订单列表与订单详情。
- 通知列表。
- 推荐活动展示。
- Mock 与真实接口切换。
- API 请求封装、错误处理和鉴权处理。

运行效果：

- 用户可以通过浏览器完成核心业务闭环。
- 商户和普通用户看到的操作入口不同。
- 后端 401 会触发前端清理登录态并跳转登录。

### 2.8 运维、监控与演示能力

已实现功能：

- Docker Compose 拉起 MySQL、Redis、Kafka、Prometheus、Grafana。
- 后端健康检查。
- Prometheus 指标采集。
- Grafana Dashboard 展示 HTTP、Worker、Kafka 等关键指标。
- JMeter 压测脚本。
- 压测数据生成脚本。
- 压测报告与截图归档。
- 实机演示和验收前运行指南。

运行效果：

- 可以在本机或验收机器上拉起完整依赖服务。
- 可以用 Grafana 展示接口吞吐、错误率、延迟分位数、Worker 处理情况和 Kafka 消费滞后。
- 可以通过小规模或中大规模压测展示系统抗压能力。

---

## 三、掌握的关键技术

### 3.1 后端技术

项目后端主要使用 Go 技术栈：

- Go 语言。
- Gin Web 框架。
- GORM ORM。
- MySQL 数据库。
- Redis 缓存与库存扣减。
- Kafka 消息队列。
- JWT 认证。
- bcrypt 密码哈希。
- Prometheus 指标暴露。
- Go test 单元测试、集成测试和 benchmark。

关键学习点：

- 如何设计 RESTful API。
- 如何进行用户认证与角色鉴权。
- 如何使用 Redis Lua 脚本保证库存扣减原子性。
- 如何使用 Kafka 解耦高并发报名请求和后端落库处理。
- 如何通过事务、幂等和状态机保证业务一致性。
- 如何通过 Go 测试覆盖 service、handler、worker 和集成链路。

### 3.2 前端技术

项目前端主要使用 React 技术栈：

- React。
- TypeScript。
- Vite。
- React Router。
- Axios。
- Vitest。
- Tailwind CSS。
- ECharts。
- i18next。

关键学习点：

- 如何组织前端路由和页面结构。
- 如何封装 API 请求与统一错误处理。
- 如何处理登录态、token 和 401 跳转。
- 如何将商户端和用户端操作入口进行角色区分。
- 如何通过 Vitest 测试 API 封装和基础页面能力。
- 如何在验收阶段关闭 Mock，使用真实后端进行联调。

### 3.3 数据与中间件技术

使用到的中间件：

- MySQL：持久化用户、活动、报名、订单、通知、行为、推荐分数等核心数据。
- Redis：缓存活动库存、报名用户集合、库存预热标记等。
- Kafka：承接报名消息，削峰填谷，降低高并发请求对数据库的直接压力。
- Prometheus：采集后端指标。
- Grafana：展示监控面板。

关键学习点：

- 数据库适合持久化事实数据。
- Redis 适合承接高频读写和库存原子扣减。
- Kafka 适合将高峰请求异步化，减少同步链路压力。
- 监控系统需要提前埋点，否则问题发生时很难解释系统行为。

### 3.4 智能技术与智能体实践

项目开发过程中使用了智能体辅助完成需求拆解、文档整理、代码实现、缺陷定位、测试补充和验收准备。

涉及的智能体相关配置和材料：

- `.codex/skills/openspec-explore`：用于需求探索与问题澄清。
- `.codex/skills/openspec-propose`：用于生成 OpenSpec change proposal。
- `.codex/skills/openspec-apply-change`：用于按 OpenSpec 任务实施变更。
- `.codex/skills/openspec-archive-change`：用于归档已完成变更。
- `.agents/skills/impeccable`：用于前端界面质量、视觉规范和交互质量提升。
- `.agents/skills/pua`：用于在多次失败或陷入停滞时推动智能体继续尝试替代方案。
- `.agents/workflows/`：用于沉淀前后端、架构、提示词和协作流程。
- `openspec/changes/`：用于管理非琐碎变更的 proposal、design、tasks 和 spec。
- `openspec/specs/`：用于保存稳定后的能力规格。
- `docs/DOCS_PROMPTS_GOVERNANCE.md`：用于记录文档与 Prompt 治理规则。

智能体实际应用场景：

- 根据课程要求和项目状态拆解 Sprint 计划。
- 辅助设计活动、报名、订单、通知、推荐等模块。
- 根据错误日志定位 MySQL、Redis、Kafka、pnpm workspace 等运行问题。
- 辅助新增 `PREHEAT` 活动预热状态与接口。
- 辅助分析报名卡在 `QUEUING` 的 Kafka 异步写入风险。
- 辅助补充安全测试和模糊测试。
- 辅助梳理 Grafana 监控面板和压测演示路径。
- 辅助生成最终总结、反思和课程建议。

实践体会：

- 智能体适合处理“信息量大、需要跨文件理解、需要形成结构化材料”的任务。
- 智能体在代码实现前必须先读取仓库上下文，否则容易提出与实际项目不一致的方案。
- 对涉及状态机、权限、并发和数据一致性的需求，需要让智能体给出测试和验证命令。
- 对文档类任务，智能体能显著提升结构化表达效率，但真实姓名、个人经历、最终截图和对话记录仍需要团队成员自己补齐。

---

## 四、开发流程与团队协作

### 4.1 整体开发流程

项目采用按 Sprint 推进的迭代方式：

1. 需求分析阶段：整理 SRS，明确用户角色、功能范围和非功能要求。
2. 架构设计阶段：设计前后端架构、数据库结构、API 契约和核心数据流。
3. 基础功能阶段：完成用户、活动、报名、订单、通知等基础模块。
4. 并发能力阶段：引入 Redis、Kafka、库存扣减、异步 worker 和压测脚本。
5. 推荐与体验阶段：补充推荐模块、前端页面和业务闭环体验。
6. 验收准备阶段：补充安全测试、模糊测试、监控、实机演示和压测展示。
7. 总结反思阶段：整理最终成果、缺陷复盘、课程建议、个人心得和配套材料。

### 4.2 各 Sprint 实际分工

项目采用按阶段动态调整分组的方式推进。不同 Sprint 根据当时的重点，在后端、前端、测试、压测、验收演示等方向重新分配人员。

#### Sprint 1 分工

| 分组 | 方向 | 成员 |
|---|---|---|
| A 组 | 后端 1 | 师东祺、李业、徐豪杰 |
| B 组 | 后端 2 | 徐浩然、潘度衡 |
| C 组 | 前端 | 周子安、金裕涵 |

Sprint 1 的重点是完成基础架构和前后端雏形搭建。后端分为两个小组并行推进，前端组负责页面框架和交互基础。

#### Sprint 2 分工

| 分组 | 方向 | 成员 |
|---|---|---|
| 后端一组 | 后端核心模块 | 钟志昊、徐豪杰 |
| 后端二组 | 后端扩展模块与联调支持 | 徐浩然、金裕涵、师东祺、潘度衡 |
| 前端 | 前端页面与接口对接 | 周子安、林滨 |

Sprint 2 的重点是扩大后端功能覆盖，并加强前后端联调。部分成员根据实际进展从前端或后端之间进行调整，提升整体推进效率。

#### Sprint 3 分工

| 分组 | 成员 |
|---|---|
| 第一组 | 徐浩然、李业 |
| 第二组 | 钟志昊、潘度衡 |
| 第三组 | 林滨、金裕涵 |

Sprint 3 的分组更偏向按功能闭环推进，各组围绕报名、订单、通知、推荐、联调测试等任务进行并行实现和验证。

#### Sprint 4 分工

| 方向 | 成员 |
|---|---|
| 全链路体验走查、万级数据承载验证与项目验收收口 | 宋嘉民 |

Sprint 4 的重点是收口和验收准备，围绕真实系统运行、万级数据承载、前后端真实联调、核心链路走查和性能表现进行集中推进。

#### Sprint 5 分工

| 方向 | 成员 | 职责 |
|---|---|---|
| 安全测试 | 林滨 | 负责安全测试、模糊测试、问题记录和回归验证 |
| 验收与实机演示 | 宋嘉民、徐豪杰 | 负责实际运行系统、演示核心功能、展示抗压能力和处理现场运行问题 |

Sprint 5 的重点是安全测试、实机演示和抗压能力展示。其中验收与演示负责人不负责验收材料和答辩材料准备，主要职责是确保系统能在真实机器上跑起来、讲得清、演示得稳。

### 4.3 汇总角色分工

从多轮 Sprint 的实际投入看，团队成员在不同阶段承担了动态变化的职责：

| 角色 | 主要职责 | 成员 |
|---|---|---|
| 后端开发 | API、业务逻辑、数据库、Redis、Kafka、Worker、并发与订单链路 | 师东祺、李业、徐豪杰、徐浩然、潘度衡、钟志昊、金裕涵 |
| 前端开发 | 页面、路由、接口封装、交互、联调与前端测试 | 周子安、金裕涵、林滨 |
| 集成与功能闭环 | 报名、订单、通知、推荐等功能闭环推进与联调验证 | 徐浩然、李业、钟志昊、潘度衡、林滨、金裕涵 |
| 性能与验收收口 | 万级数据、全链路体验、实机运行与验收准备 | 宋嘉民 |
| 安全测试 | 安全授权测试、模糊测试、风险记录和修复验证 | 林滨 |
| 实机演示 | 真实机器运行、核心功能演示、压测与监控展示 | 宋嘉民、徐豪杰 |

### 4.4 Sprint 6 报告分工

Sprint 6 的目标是完成课程要求的总结与反思报告，不再额外分配文档结构治理、测试核查或验收复现类工作。分工依据前几轮实际参与方向安排，允许多人负责同一报告的不同部分。

| 报告内容 | 负责人 | 说明 |
|---|---|---|
| Sprint 6 总体报告范围与进度把控 | 李浩博 | 作为全组组长，负责确定 Sprint 6 的报告范围、分配各专题负责人、把控最终总结材料的推进节奏 |
| 项目最终功能模块与运行效果报告 | 宋嘉民、徐豪杰 | 只需要整体展示项目最终完成的功能模块及运行效果，不要求每个人分别介绍 |
| 每位成员个人实现说明 | 全体成员 | 每个人在个人心得中说明自己提交代码实现了什么、位于项目哪个模块、对整体系统起到什么作用 |
| 关键技术与智能技术报告 | 全体成员 | 每个人都需要介绍自己掌握和使用的技术栈，以及自己使用智能体、MCP Server、规则文档、Skills、对话记录等智能技术的实际场景 |
| 开发流程与团队协作报告 | 宋嘉民、林滨 | 只做整体说明，描述 Sprint1 到 Sprint5 的开发流程、团队分工、沟通机制与协作模式，不要求每个人分别介绍 |
| 个人心得体会 | 全体成员 | 每位成员分别撰写角色、自己提交代码实现了什么及其项目位置、主要挑战、解决方法、收获与深度反思 |
| 压力测试专题报告 | 钟志昊、潘度衡 | 对应 Sprint3 第二组的 JMeter、Grafana 面板与压测相关工作，说明测试方法、工具选型和性能表现；宋嘉民、徐豪杰可补充 Sprint5 演示视角 |
| 微服务架构专题报告 | 李浩博 | 从组长和架构复盘视角说明当前项目未完全微服务化的原因、潜在拆分策略、服务间通信机制和推荐技术路径 |
| 系统监控专题报告 | 钟志昊、潘度衡 | 结合 Prometheus、Grafana、HTTP 指标、Worker 指标和 Kafka lag 进行介绍；宋嘉民、徐豪杰可补充验收演示中的观察方式 |
| 弹性设计专题报告 | 徐浩然、钟志昊、潘度衡、李业 | 由后端相关同学介绍 Redis 原子扣减、Kafka 削峰、幂等控制、订单过期回滚，以及未来断路器、重试、降级方向 |
| 安全性保障专题报告 | 林滨、徐豪杰、周子安 | 林滨结合 Sprint5 安全测试与模糊测试介绍；徐豪杰、周子安补充后端鉴权、前端登录态和 XSS 防护等安全措施 |
| 单元测试报告 | 写对应测试的同学 | 介绍单元测试覆盖范围、运行方式和结果；按实际测试提交记录补充具体姓名 |
| 集成测试报告 | 写对应测试的同学 | 介绍集成测试覆盖的业务闭环、运行方式和结果；按实际测试提交记录补充具体姓名 |
| 压力测试日志 | 钟志昊、潘度衡 | 介绍压测脚本、压测日志、Grafana 指标和报告结论 |
| 安全测试结果 | 林滨 | 介绍安全测试、模糊测试、发现问题、剩余风险和改进方向 |
| 智能体配置与对话记录说明 | 全体成员 | 每个同学都需要介绍自己使用的智能体配置、MCP Server 设置、规则文档、Skills，以及每轮对话交互过程记录 |

本阶段只要求各负责人围绕报告内容补充真实经历、事实依据和个人反思；已有测试报告、压测日志、安全测试结果、对话记录等材料作为报告支撑引用。若某一项需要“按实际提交记录补充具体姓名”，由对应代码或测试提交者在最终填写时确认。

### 4.5 沟通机制

项目协作主要依赖以下机制：

- 使用 Sprint 文档明确阶段目标、任务范围和负责人。
- 使用 `docs/README.md` 作为文档入口，减少材料散落。
- 使用 `docs/SYSTEM_DESIGN.md` 记录稳定架构和 API 契约。
- 使用 OpenSpec 管理非琐碎变更，避免需求口头漂移。
- 使用测试命令和运行指南作为联调依据，而不是只依赖口头说明。
- 对复杂问题先复现，再定位，再修复，再运行测试验证。

### 4.6 协作中暴露的问题

主要问题：

- 文档与代码同步有时滞后，导致后续成员需要重新确认真实实现。
- 早期任务边界不够清晰，容易出现某些模块无人持续跟进。
- 部分功能先实现后补测试，导致验收前补测压力较大。
- 前端、后端、测试和演示之间的运行环境要求没有一开始就统一，后期出现端口、数据库、pnpm workspace、seed 数据等问题。
- 对 Kafka、Redis、Prometheus、Grafana 等中间件的依赖关系，早期没有完全用图或脚本固化，导致新人理解成本较高。
- 智能体生成内容需要人工审核，若直接采用，可能出现路径不一致、命令不适配或材料过度泛化。

改进方向：

- 每个 Sprint 结束时同步更新代码、测试、运行说明和已知风险。
- 每个功能合并前补齐最小测试，不把所有测试压力留到验收前。
- 关键链路固定一份“从零拉起系统”的脚本和检查清单。
- 对智能体输出建立人工复核机制，尤其是状态机、权限、并发和安全相关内容。
- 让每个成员至少掌握系统启动、核心业务闭环和常见故障处理，而不是只了解自己负责的文件。

---

## 五、个人心得体会


### 5.1 师东祺

在项目中承担的角色：

- Sprint 1 后端 1 组（A 组）成员：负责为活动、报名、订单三大核心业务模块的 HTTP 接入层（handler）补齐单元测试，为 A 组同步推进的 service 层实现兜底回归。
- Sprint 2 第二组「同学甲」：前期负责前后端联调入口切换（关闭 MSW、对齐 `.env`），末期负责 JMeter 压测脚本编写、压测执行与全组 DoD 统一验收检查。

自己提交代码实现了什么，在项目中的位置：

- 我的提交主要覆盖后端 handler 层的单元测试和 Sprint 2 末期的压测/验收材料两块，对应仓库中的 `backend/internal/handler/*_test.go` 与 `tools/jmeter/` 两处。
- `backend/internal/handler/activity_handler_test.go`：513 行，围绕活动模块 handler 编写 stub service 与表格化用例，覆盖创建、编辑、预热、发布、列表、详情、库存、商户列表等接口，覆盖 200/201/400/404 全套响应分支，并验证 `merchant_id`、`category`、`sort`、`page` 等参数透传是否正确。
- `backend/internal/handler/enrollment_handler_test.go`：435 行，针对报名 handler 测试 `Create / GetStatus / ListByUser / Cancel` 四类接口，重点覆盖 202 排队受理（`code=1201`）、200 库存不足（`code=1101`）、409 重复报名、状态查询的活动/订单联表回包，确保契约与前端拦截器解析口径完全对齐。
- `backend/internal/handler/order_handler_test.go`：375 行，覆盖订单列表、订单详情、支付、过期扫描 4 类 handler 方法，测试支付成功、订单不存在、订单状态非法、用户越权读他人订单等典型分支，固化"用户只能看到/支付自己订单"这一安全口径。
- `tools/jmeter/enrollment-load.jmx`：报名接口压测计划，使用 CSV 读取 token、Bearer 注入 Header、`POST /api/v1/enrollments` 直击抢票主链路，配置 100 并发基线线程组（Ramp-Up 10s）与可切换的 1000 并发峰值线程组，断言把 202/200/409 这三种"业务可接受"响应明确区分于 5xx 系统失败。
- `tools/jmeter/ACCEPTANCE_CHECKLIST.md`：249 行 Sprint 2 全组统一验收清单，逐条对应 Sprint 2 DoD：环境与启动检查、Chrome DevTools Network 检查（含 MSW 关闭确认）、C 端核心链路、商户端、通知模块、压测与监控、契约与字段对齐、边界与异常场景，以及压测后 Redis/MySQL 库存一致性核对、Grafana 曲线观测、常见问题排查共十节。
- `tools/jmeter/jmeter.log`、`results-100.jtl`、`report-100/`、`tokens.csv`（部分未入库）：100 并发基线压测的执行日志、原始结果与 HTML 报告，作为 Sprint 2 第二组联调验收的实测证据。
- 这些代码与材料共同支撑了"A 组 handler 层有回归测试兜底"与"Sprint 2 第二组联调阶段有可复跑的压测和清单式验收"两件事。

遇到的主要挑战：

- handler 层是业务规则与 HTTP 协议的边界，既要验证 Gin 路由、参数绑定、状态码与响应体序列化，又不能把测试做成 service 的重写——如果没有合理的 stub 设计，handler 测试就会变成"测假数据回真数据"的空壳，覆盖率高但抓不到回归。
- 报名接口的契约比一般 CRUD 复杂：同一个 `POST /enrollments` 在"排队受理 / 库存不足 / 重复报名"三种业务状态下分别返回 202+1201、200+1101、409，前端拦截器、JMeter 断言、handler 测试必须用同一套口径，否则一处改动会三处出错。
- Sprint 2 末期做 JMeter 压测时，token 是真正的难点：token 写进 `.jmx` 既不安全也无法在另一台机器复跑，并发 100 时 token 不足又会让 401 错误率压过真实业务错误，无法判断是系统瓶颈还是数据准备不到位。
- 统一验收阶段需要在乙、丙、丁三人各自合入的代码上做"端到端检查"，没有清单化就只能靠口头确认，验收时漏一项、合并后又冒出一项的情况非常容易出现。

采取的解决方法：

- handler 测试采用「stub service + 表驱动」模式：为 `ActivityService`、`EnrollmentService`、`OrderService` 三个接口分别写了带 `lastUserID / lastActivityID / lastPage` 等回写字段的 stub，既能注入想要的返回值（成功结果、`ErrInvalidTimeRange`、`ErrActivityNotFound`、`ErrInvalidActivityState` 等领域错误），又能反向验证 handler 把 URL 参数、登录态、Query 透传到了 service，确保测试既覆盖响应又覆盖参数解析。
- 把后端 handler、前端 axios 拦截器、JMeter 断言三处的契约统一对齐到 `pkg/response`：在 handler 测试里固定 202/200/409 三套断言；在 ACCEPTANCE_CHECKLIST 里把"202+1201/200+1101/409/401/404"做成对照表；在 `.jmx` 里把这三种响应都计入"业务正常"，只对 5xx 计错。这样无论谁先改、谁后改，契约只有一个真理来源。
- JMeter token 用 `CSVDataSet` 外置到 `tokens.csv`（不入库），`shareMode=all` + `recycle=true` 允许 token 数小于线程数时循环复用；`ACTIVITY_ID` 通过 `TestPlan.arguments` 提取为变量，方便另一台机器只替换两处参数就能复跑——配合 `enrollment-load.jmx` 顶部注释、`ACCEPTANCE_CHECKLIST` 第五节的步骤说明、Q1～Q5 常见问题排查，形成一份可移交的压测套件。
- 在 Sprint 2 末期把验收做成「九节清单 + 勾选 + 备注」：每个 DoD 子项都拆成最小可勾选粒度（如"Network 中无 MSW 标记"、"压测后 `SELECT COUNT(*) FROM enrollments WHERE status='SUCCESS' <= MaxCapacity`"），让乙、丙、丁交付的代码必须能被同一份清单逐条核对，而不是"我这边能跑"。

最终收获：

- 真正建立了"handler 层应该测什么、不该测什么"的判断：handler 只验证"协议层正确"（状态码、参数解析、错误映射、响应体结构），业务逻辑交给 service 层测试，避免重复，也避免漏检。
- 学会了用 stub 模拟领域错误（`service.ErrXxx`）来驱动 handler 的错误分支测试，这套写法比 mock 框架更轻、更可读，也更能体现"测的是边界，不是实现"。
- 掌握了 JMeter 从计划编写、CSV 参数化、断言判定到 HTML 报告生成 (`-n -t ... -l ... -e -o ...`) 的完整链路，体会到压测脚本本身就是产物，要让它"在任意机器替换两个参数即可复跑"。
- 通过当 Sprint 2 第二组的"验收点"，更深地理解了 DoD（Definition of Done）的意义：把"完成"从主观变成可勾选，把"对齐"从口头变成 review 凭据，这件事在多人协作里比写代码还重要。

深度反思：

- 单元测试不是"覆盖率达标"就够，handler 测试如果只测 200 OK 而不测 400/404/409/422，等于没测——业务错误和系统错误的分流恰恰发生在 handler 层，这是回归最容易漏的盲区。我后续在补 handler 测试时会先列错误分支再写正例，避免被"成功路径"主导。
- 压测和单元测试不是两套体系：handler 测试里固化下来的契约（什么时候 202、什么时候 200+code、什么时候 409），就是 JMeter 断言的依据。如果一开始把测试当成"写完代码再补的麻烦事"，到了压测阶段就会发现脚本和后端口径对不上、又回头改 handler，反复消耗。先有清晰契约，再有测试，再有压测脚本，顺序很重要。
- "同学甲"这个角色给我的最大启发是：在多人并行开发的项目里，比"自己写得多"更稀缺的是"让别人写出来的东西能被一致验证"。Sprint 2 末期我没有再去抢业务代码，而是把入口（`.env`/MSW）和出口（`.jmx`/`ACCEPTANCE_CHECKLIST`）做扎实，反而让整组的联调速度更快、合并冲突更少——这让我重新认识了"基础设施型贡献"的价值。
- 智能体辅助让我能更快搭起 handler stub 与 JMeter `.jmx` 骨架，但契约口径（202+1201 还是 200+1201？库存不足是 200 还是 409？）智能体经常给出"看起来合理"的答案，必须回到 `pkg/response`、`enrollment_handler.go` 和 SYSTEM_DESIGN 才能锁死——这件事再次提醒我：智能体擅长结构与模板，业务真理只能在代码与文档里找。

### 5.2 李业

在项目中承担的角色：

- Sprint 1 后端 1 组成员，负责活动（Activity）、报名（Enrollment）、订单（Order）三个核心模块从零搭建。
- Sprint 3 第一组成员，负责报名抢票链路的闭环收口：异步落盘 Worker、订单过期、报名取消、活动逾期下线与行为驱动热度。

自己提交代码实现了什么，在项目中的位置：

- 我的代码位于后端 `internal/service`、`internal/worker`、`internal/handler` 三层，是系统抢票主链路的核心实现。
- `enrollment_service.go`：报名下单全流程（前置校验 → Redis 原子扣减 → 落库 QUEUING → 投递 Kafka），以及基于状态机的报名取消。
- `order_service.go`：订单支付（乐观锁状态流转）与订单过期扫描 `ScanExpired`（超时未支付自动关闭，并在 MySQL 与 Redis 双端回补库存、通知用户）。
- `enrollment_worker.go`：消费 Kafka 报名消息，在一个事务里完成"报名转 SUCCESS → 生成 PENDING 订单（15 分钟过期）→ enroll_count 自增"，事务失败时补偿回滚 Redis 并标记 FAILED、通知失败。
- `activity_offline_job.go`：幂等批量扫描，把 `activity_at` 已过且非终态的活动转为 OFFLINE。
- 这几块共同支撑了 SRS 中"零超卖红线"与"最终一致性"两条非功能需求的落地。

遇到的主要挑战：

- 高并发下的防超卖与幂等：判断库存、扣减、防重复报名、取号必须是不可分割的原子操作，否则并发下会超卖或一个用户占两个名额。
- 异步链路的一致性：接口为削峰先返回 QUEUING、真正落库交给 Worker，一旦 Worker 落库事务失败，已扣减的 Redis 库存必须补偿回去。
- 多套状态机交织的竞态：报名、订单、活动三套状态机交织，取消、过期、支付可能并发触及同一条记录。

采取的解决方法：

- Redis Lua 原子扣减：把"SISMEMBER 判重 → GET 库存 → DECRBY 扣减 → SADD 入集合 → INCR 取号"写进一段 Lua，由 Redis 单线程保证原子性，用返回值 `-2/-1/正数` 区分"重复报名/库存售罄/成功取号"，杜绝线程锁与数据库锁表。
- 全链路补偿（Saga 思路）：报名落库失败、Kafka 投递失败、Worker 事务失败、订单过期、报名取消每条异常路径都调用 `Rollback`（INCR 库存 + SREM 集合）把库存补回。
- 乐观锁替代悲观锁：遵循"禁止 `SELECT ... FOR UPDATE`"约定，所有状态流转用带条件的原子更新（如 `WHERE status='QUEUING'`、`enroll_count < max_capacity`），靠 `RowsAffected` 判断是否抢到流转，天然幂等、无锁竞争。
- 逾期下线用幂等 WHERE 批更新，重复执行第二遍自然 0 行更新，可安全周期调度。

最终收获：

- 真正理解了"缓存扛并发、DB 保最终一致、MQ 做削峰"这套高并发抢票架构是如何协作的，而不只是名词。
- 学会了在分布式异步链路里用补偿/回滚而非分布式事务来保证一致性，体会到每条失败路径都要想清楚"库存回没回去"。
- 掌握了用乐观锁 + 原子条件更新实现无锁幂等状态机，明白了"幂等不是加个判断，而是让操作天然可重复"。
- 学会了为并发逻辑写可复现的测试：多 goroutine 验证 stock=1 只有 1 个成功、用可注入时钟绕过定时器测过期。

深度反思：

- 异步系统不能只看接口返回：接口返回"排队成功"不代表消息真的进了 Kafka、真的落了库；"最佳努力"的补偿若也失败，记录仍会卡在 QUEUING，需要对账兜底——可靠性是分层的，没有银弹。
- 状态机要先画清楚再写代码：三套状态机交织时，先穷举合法迁移再编码，比边写边补判断更不容易漏掉竞态。
- 先测试后实现很有价值：并发与过期问题靠肉眼 review 很难发现，是测试把"看起来对"变成"可证明对"。

### 5.3 徐豪杰

在项目中承担的角色：

- Sprint 1 后端 1 组成员；Sprint 2 后端一组成员；Sprint 5 验收与实机演示负责人之一。

自己提交代码实现了什么，在项目中的位置：

- 实现了后端的订单相关功能接口，包括订单创建、状态更新、历史查询等核心模块。在开发过程中，对项目后端代码进行了初步的单元测试与集成测试，保障了代码质量。在 Sprint 5 阶段，负责统筹验收流程与实机演示，确保项目顺利通过评审。整体上，我主要负责项目的后端开发与最终交付两个关键环节。

遇到的主要挑战：

- 对 Go 语言的并发模型（goroutine、channel 等）掌握不够熟练，开发初期效率较低。

- 对 Redis 缓存策略的理解不够深入，初期设计的缓存方案存在缓存穿透风险。

- 对 Kafka 消息队列的生产者/消费者机制不够熟悉，曾遇到消息重复消费的问题。

采取的解决方法：

- 借助 AI Agent 进行辅助学习，快速掌握 Go 语言、Redis 和 Kafka 的核心知识点。

- 利用 Agent 拆解复杂概念，通过交互式对话理解 Goroutine 与 Channel 的最佳实践。

- 参考 Agent 提供的典型场景，重构了订单缓存的 "旁路缓存"模式，并基于 Kafka 实现了可靠的异步日志机制，解决了消息重复消费问题。

最终收获：

- 完整经历了大型项目从开发到交付的全流程，熟悉了敏捷开发（Sprint 迭代）的协作模式。

- 掌握了 Go 语言、Redis、Kafka 在真实业务场景中的落地应用。

- 锻炼了跨角色协作能力，特别是在 Sprint 5 担任验收负责人，提升了项目把控与沟通协调能力。

深度反思：

- 这次经历让我认识到，"以用促学"是工程化学习最有效的路径。面对 Redis、Kafka 这类成熟但复杂的组件，与其从头通读文档，不如先在 AI Agent 辅助下完成最小可行产品，再回头深究底层原理——这种"先做后学"的方式大幅提升了我的学习效率。此外，从 Sprint 1-2 的"代码实现者"到 Sprint 5 的"验收负责人"，我最大的转变是学会了从用户视角定义"完成"的标准。技术功能跑通不等于项目成功，真正的交付需要考虑边缘场景、异常处理和用户体验。未来在开发前期，我会更早地引入验收思维。

### 5.4 徐浩然

在项目中承担的角色：

- Sprint 1 后端 2 组成员；Sprint 2 后端二组成员；Sprint 3 第一组成员。
- Sprint 6 弹性设计专题报告负责人之一，重点梳理报名抢票链路中的 Redis 原子扣减、Kafka 异步削峰、幂等状态流转和库存回滚补偿。

自己提交代码实现了什么，在项目中的位置：

- 我的工作主要集中在后端报名、库存和订单闭环，相关代码位于 `backend/internal/service`、`backend/internal/worker`、`backend/internal/repository` 以及 `backend/tests`。
- `stock_lua.go` 抽象了 Redis 库存引擎，通过 Lua 脚本把重复报名判断、库存扣减、报名用户集合写入和排队序号生成放在一个 Redis 原子操作中，支撑高并发报名入口。
- `enrollment_service.go` 负责报名请求的前置校验、Redis 扣减、`QUEUING` 报名记录创建、Kafka 消息投递，以及投递失败时的 Redis 回滚和报名失败标记。
- `enrollment_worker.go` 负责消费 Kafka 报名消息，在 MySQL 事务中将报名从 `QUEUING` 推进到 `SUCCESS`、创建 `PENDING` 订单并增加活动报名计数；事务失败时补偿 Redis 并发送失败通知。
- `order_service.go` 的订单支付和 `ScanExpired` 过期扫描通过条件更新控制状态流转，过期订单关闭后同时回补 MySQL 活动库存和 Redis 实时库存，避免名额被长期占用。
- 配套测试覆盖了 Redis Lua 扣减与回滚、Worker 幂等处理、订单过期回滚、取消与过期并发场景，帮助验证抢票链路在异常路径下不会明显超卖或重复回补。

遇到的主要挑战：

- 抢票链路的入口并发压力集中，若直接依赖数据库查询和更新库存，容易在高并发下出现锁竞争、超卖或响应时间抖动。
- 报名接口采用异步削峰后，请求返回 `QUEUING` 并不代表订单已经创建成功，需要处理 Redis 已扣减但 Kafka 投递失败、Worker 事务失败、用户主动取消、订单过期等多条异常路径。
- 幂等控制不仅要防止同一用户重复报名，还要防止 Worker 重复消费、状态重复流转、取消和过期扫描并发触发时造成重复回滚。
- Redis、Kafka、MySQL 三类组件分别承担实时库存、消息缓冲和最终持久化职责，任何一层失败都可能造成状态不一致，必须明确补偿边界和可观测信号。

采取的解决方法：

- 使用 Redis Lua 脚本作为报名入口的第一道闸门，一次性完成 `SISMEMBER` 判重、`GET` 库存检查、`DECRBY` 扣减、`SADD` 记录用户和 `INCR` 生成排队序号，避免把高并发库存竞争直接打到 MySQL。
- 使用 Kafka 将“接收报名请求”和“创建订单落库”解耦，接口层只负责完成准入和消息投递，Worker 层异步完成报名成功、订单创建和活动计数更新，削平瞬时流量峰值。
- 对所有关键状态流转采用条件更新，例如报名只从 `QUEUING` 更新为 `SUCCESS`，订单只从 `PENDING` 更新为 `PAID` 或 `CLOSED`，并根据 `RowsAffected` 判断是否真正抢到状态流转权。
- 设计补偿回滚路径：报名记录创建失败、消息序列化失败、Kafka 写入失败、Worker 事务失败、用户取消和订单过期都会调用 Redis `Rollback`，将库存加回并从已报名集合移除用户。
- 用测试复现容易被忽略的竞态场景，例如订单过期扫描重复执行、取消与过期并发执行、Worker 收到非 `QUEUING` 报名消息，确保重复调用不会造成重复扣减或重复回补。

最终收获：

- 理解了弹性设计不是单一技术点，而是 Redis、Kafka、MySQL、Worker、补偿逻辑和监控指标共同组成的工程体系。
- 掌握了用缓存承接热点写入、用消息队列削峰、用数据库状态机保证最终一致的典型高并发业务架构。
- 对幂等有了更实际的认识：幂等不是简单判断“是否存在”，而是要让每个状态迁移都有明确前置条件，并允许消息重放、任务重跑和接口重试。
- 意识到测试对并发链路非常重要，尤其是库存、订单和取消这类涉及资源归还的逻辑，必须通过可重复测试确认异常路径。

深度反思：

- 当前系统已经能通过 Redis 与 Kafka 承接一定并发，但弹性能力仍偏“应用内补偿”，还缺少更完整的重试队列、死信队列、断路器和统一降级策略。
- Worker 失败后虽然会尽量回滚和标记失败，但如果补偿本身失败，仍需要后续对账任务扫描 `QUEUING`、Redis 库存和 MySQL 订单之间的差异，形成更稳的兜底机制。
- 报名链路在设计初期应先画清楚库存、报名、订单三套状态机，以及每个失败点对应的补偿动作；先写代码再补边界条件，容易遗漏异常分支。
- 后续如果继续演进，我会优先补充 Kafka 消费重试与死信队列、Redis/MySQL 库存对账、核心接口限流和推荐等非核心接口降级，让系统从“能抗压”进一步变成“故障下可恢复、可观测、可解释”。

### 5.5 潘度衡

在项目中承担的角色：

- Sprint 1 后端 2 组成员；Sprint 2 后端二组成员；Sprint 3 第二组成员。

自己提交代码实现了什么，在项目中的位置：

- 我负责通知模块、行为埋点模块、B 组后端基础设施，以及前后端联调和压测支持，代码主要位于后端 `internal/repository`、`internal/service`、`internal/handler`、`internal/config` 和前端商户、通知相关页面。通知模块实现通知列表、未读数和标记已读，并向报名、订单等主流程提供报名成功、报名失败、订单过期、活动提醒等事件反馈；行为埋点模块实现用户行为的单条与批量上报，为推荐模块的热度计算提供数据；基础设施部分完成 MySQL 接入、数据库连接池配置和 bgroup 黑盒测试套件。联调方面把商户活动管理、通知列表和通知铃铛从 Mock 切换到真实后端接口；压测方面搭建了压测账号生成脚本、报名接口的 JMeter 压测计划和报告脚本，并整理了联调验收清单，同时把注册接口限流改为环境变量可配，方便压测前批量准备账号。

遇到的主要挑战：

- 通知和行为属于业务旁路模块，写入失败不能影响报名、订单等主流程。
- 行为埋点写入频繁，同步落库会拖慢主接口，异步写入又要避免丢数据。
- 压测需要大量真实账号，但注册接口有限流，直接批量注册会被拒绝。
- 前端从 Mock 切到真实接口时，分页结构和字段容易和后端契约不一致。

采取的解决方法：

- 通知写入采用 best-effort，失败只记录日志、不影响主流程。
- 行为写入做成同步/异步可配，兼顾接口耗时和数据可靠性。
- 压测脚本顺序注册并在被限流时退避，再把注册限流改为可配置、压测前临时放宽。
- 联调以系统设计文档的接口契约为准逐页核对，并把核对项写进验收清单。

最终收获：

- 理解了通知、行为这类旁路模块的设计取舍，知道如何在不影响主链路的前提下可靠地记录业务事件，也明白了 best-effort 写入与强一致写入分别适用的场景。
- 熟悉了 MySQL、连接池配置和 bgroup 测试在后端服务中的协作方式，体会到统一配置和测试覆盖对系统稳定性的作用。
- 完整掌握了从压测账号生成、JMeter 压测计划编写到报告产出的流程，学会了通过限流配置、参数化数据来让压测更接近真实场景。
- 通过前后端联调，更深入理解了接口契约、分页结构和错误码在前后端协作中的重要性，也提升了定位和排查联调问题的能力。

深度反思：

- 旁路模块要先想清楚是否允许失败，best-effort 和强一致是两种不同的设计。
- 压测不只是跑脚本，更要提前准备数据、放开限流，并区分业务失败和系统失败。
- 把联调契约固化成清单比口头确认更能减少返工。

### 5.6 周子安

在项目中承担的角色：

- Sprint 1 前端组成员；Sprint 2 前端组成员。

自己提交代码实现了什么，在项目中的位置：

- 我的代码集中在 `frontend/src`，覆盖公开站点体系、抢票闭环界面和全站统一重构，是用户通过浏览器接触整个系统的主要入口。
- 公开发现页体系：`pages/Home.tsx`、`pages/PublicActivities.tsx`、`pages/Notifications.tsx`，以及 `components/public/` 下的轮播（BannerCarousel）、分类条（CategoryStrip）、公共头部（PublicHeader）、分页、推荐列表、通知铃铛、空态、加载骨架等公共组件。
- 请求与 Mock 双层架构：`api/endpoints/` 按活动、推荐、通知、报名、订单等业务域拆分请求层，`mocks/handlers/` 提供一一对应的 MSW handler，通过 `VITE_USE_MOCK` 开关切换，使前端能在后端未就绪时并行开发、联调时零改动切换真实接口。
- 首页 Hero 与中国地图选城体验：引入 ECharts 与全国及省级 GeoJSON（`public/geojson/`），实现按地域发现活动的地图交互，并重设计首页 Hero 区。
- 抢票闭环前端：`api/endpoints/enrollments.ts`、`orders.ts` 请求层，`ActivityDetail.tsx` 报名入口、`EnrollStatus.tsx` 排队结果页、`Orders.tsx` / `OrderDetail.tsx` 订单流转。这是后端“Redis 原子扣减 + Kafka 异步落库”链路在用户侧的呈现：报名接口返回 `QUEUING` 排队状态后，由状态页轮询最终结果。
- 全站统一重构：删除早期 Dashboard 双轨页面体系（`Dashboard.tsx`、`Activities.tsx`、`DashboardLayout.tsx`、`MerchantLayout.tsx` 等共 1100 余行），把用户端与商户端统一收口到公开站点 + `ProtectedRoute` 受保护路由结构，并保留 `/app/*`、`/dashboard` 等旧路径重定向兼容。
- 工程基建：中英文 i18n（`i18n/locales/`）、业务类型体系（`types/`）、Liquid Glass 视觉风格，并向 `.claude/skills/`、`.agents/skills/` 引入 `ui-ux-pro-max` 与 `impeccable` 两个前端设计 skill，作为团队共享的 AI 工作流资产。

遇到的主要挑战：

- 双轨页面体系的债务：早期同时存在“后台 Dashboard 风”和“公开站点风”两套布局与页面，用户端、商户端入口割裂，样式与状态管理各自为政，越往后合并冲突越多。
- 后端未就绪下的并行开发：前期只有设计文档里的接口契约，没有真实数据可调，又要保证日后切换真实后端时不返工。
- 多人并行的集成冲突：`dev` / `master` 双分支加多名队友 PR，多次出现三方合并冲突，需要在合并时保证前端不被破坏。
- 全程双语与复杂可视化：每个功能提交都要同步维护 `zh.json` / `en.json` 文案；地图体验需要处理 ECharts 按省 GeoJSON 的加载与交互。

采取的解决方法：

- 一次性统一重构：净删 700 余行旧双轨代码，所有页面收口到统一的公开站点 + 受保护路由结构，用旧路径重定向保证兼容，而不是无限缝补两套体系。
- Mock 先行 + 分层请求架构：endpoints 与 MSW handlers 一一对应，接口形状对齐 `docs/SYSTEM_DESIGN.md` 的 API 契约，并固化 Mock 测试账号方便队友自测；联调时只需关闭 Mock，业务代码零改动。
- 小步聚焦提交：按“发现页 → 地图 → 视觉 → 端整合 → 抢票 → 全站统一”的顺序推进，合并队友 PR 后立即跟进 lint 等修复，保持 `dev` 分支始终可用。
- 组件抽取复用：把轮播、分页、空态、通知铃铛等沉淀为 `components/public/` 公共组件，商户端辅件沉淀到 `components/merchant/`，新页面以拼装为主。

最终收获：

- 亲历“双轨页面 → 统一站点”的重构，理解了前端路由架构与布局分层（`layouts/` + `ProtectedRoute`），体会到删代码比加代码更需要勇气和全局视野。
- 掌握了契约驱动的 Mock 先行开发模式，前后端并行互不阻塞，联调切换成本接近零。
- 作为仓库 Owner 实操了分支策略、PR 审阅合并与冲突解决，体会到“频繁小步合并 + 合并后立即修复”对团队节奏的价值。
- 认识到 i18n、类型体系、lint 基线、gitignore 卫生这类“不出彩”的工程工作，决定了项目后期还能不能顺畅迭代。

深度反思：

- 应用大模型 / Agent 不能盲目“一句话生成”：复杂任务先用 Plan 模式做系统规划，长链路工作用 Ralph Loop 让智能体不间断推进，产出再经 code review / 审计类 skill 复核把关。实践中发现智能体很擅长“再加一个页面”，但不擅长“该删掉哪套体系”——统一架构、接口契约这类取舍性决策仍然完全依赖人对全局的理解。工具放大执行力，不替代判断力。
- 前端品质需要持续汲取先进设计思想和优秀页面的思考方式：本项目通过引入 Liquid Glass 风格与 `impeccable`、`ui-ux-pro-max` 等设计 skill，把优秀页面的设计经验固化为团队可复用的资产，而不是停留在个人审美层面。
- 大规模系统和普通规模系统对前端的要求不同：普通项目里前端只管“调接口、渲染数据”；而在抢票这种高并发场景下，前端必须配合后端的异步架构——报名返回的是排队状态而非最终结果，需要状态页轮询、超时提示和降级展示，Mock 也必须严格对齐契约，否则联调阶段的细微偏差会被并发场景放大。
- 架构分叉的窗口期很短：发现两套并行页面结构的当天就应该做取舍，而不是等“有空了再统一”——推迟的决策成本最终都变成了集中重构的成本。

### 5.7 金裕涵

在项目中承担的角色：

- Sprint 1 前端 C 组成员，负责登录页、商户活动管理、活动详情页等 B 端/C 端核心页面。
- Sprint 2 后端二组成员，参与扩展模块联调；实际提交以 Sprint 1 前端主链路的 API 对接与页面完善为主。
- Sprint 3 第三组成员，与林滨共同推进前端交互闭环，负责用户行为埋点的前端接入。

自己提交代码实现了什么，在项目中的位置：

- 我的代码主要位于 `frontend/src/pages`、`frontend/src/api/endpoints`、`frontend/src/components` 和 `frontend/src/mocks`，覆盖商户端活动管理与推荐链路所需的前端入口。
- **Sprint 1 商户活动管理链路**（`687a3c7`）：实现 `MerchantLayout`、`MerchantDashboard`、`MerchantActivities`、`MerchantActivityNew`、`MerchantActivityEdit` 及可复用的 `MerchantForm`，配套 `auth.ts`、`activities.ts` API 封装与 MSW Mock，使商户可以完成活动创建、编辑、列表查看与发布操作，并更新 `docs/SPRINT1.md` 中 C 组个人任务说明。
- **Sprint 2 前后端对接与 C 端体验**（`c354163`）：重构 `Home`、`Activities`、`ActivityDetail`，将首页热门推荐和活动列表从 Mock 切换为真实 API；在 `ActivityDetail` 中补充报名排队轮询与倒计时展示；增强 `AuthContext` 的远程登出与本地存储清理，打通用户从浏览到报名的前端闭环。
- **Sprint 3 行为埋点模块**（`ebae612`）：新增 `behaviors.ts`，实现 VIEW/COLLECT/SHARE/CLICK/SEARCH 等行为的单条上报、批量队列与定时 flush；在 `ActivityDetail` 接入浏览埋点，为后端推荐模块的热度计算与用户行为分析提供前端数据来源；行为上报采用 fire-and-forget 策略，失败不阻塞报名、登录等主流程。

遇到的主要挑战：

- Sprint 1 阶段后端接口尚未全部就绪，商户页面需要在 MSW Mock 与真实契约之间反复对齐，字段命名（camelCase / snake_case）和分页结构容易不一致。
- 商户端涉及角色守卫、路由跳转和表单校验，创建活动时还要满足 `enroll_open_at < enroll_close_at < activity_at` 等业务时间逻辑，任何一处遗漏都会导致联调返工。
- Sprint 2 从 Mock 切到真实 API 后，`ActivityDetail` 的报名状态从同步成功变为 `QUEUING` 排队，前端需要补充轮询、错误态和禁用态，否则用户无法感知异步抢票进度。
- Sprint 3 行为埋点属于旁路能力：上报频繁但不能拖慢页面，也不能因为 401 重定向打断用户正在进行的操作。

采取的解决方法：

- 按 Sprint 1 文档约定的文件边界小步提交，先完成 `auth.ts` 与 `activities.ts` 类型和 API 封装，再实现 `MerchantForm` 和商户各页面，最后统一 Mock handler，减少与周子安等其他前端同学的冲突。
- 商户表单把创建与编辑共用 `MerchantForm`，列表页通过 route state 传递操作反馈，发布成功后即时刷新状态标签，保证 B 端操作链路可感知。
- 活动详情页区分 Skeleton、空态、错误态和库存颜色态；报名按钮根据活动状态、库存和登录态动态禁用，并对 `QUEUING` 状态做轮询查询。
- 行为埋点模块采用内存队列 + 批量上报（满 10 条或 60 秒 flush），请求附带 `AUTH_REDIRECT_BYPASS_HEADER` 避免 401 时强制跳转登录；所有上报用 `catch` 静默处理，确保旁路失败不影响主业务。

最终收获：

- 完整经历了从 Mock 驱动开发到真实 API 联调的前端工程流程，理解了“先契约、再页面、最后联调”的顺序为什么不能颠倒。
- 掌握了 React + TypeScript 下商户端多页面、表单复用、路由守卫和 i18n 的组织方式，也熟悉了 Axios 封装、MSW Mock 与 Vitest 测试在项目中的协作关系。
- 通过行为埋点模块，理解了推荐系统前端侧的职责边界：只负责可靠、低侵入地采集事件，把计算和排序交给后端。
- 在 Sprint 1 前端、Sprint 2 联调、Sprint 3 闭环三个阶段中，逐步从“把页面做出来”转变为“把链路跑通并考虑异常路径”，对全栈项目的协作节奏有了更实际的体会。

深度反思：

- 前端不能只看页面是否渲染，还要和后端状态机对齐：活动 `DRAFT/PREHEAT/PUBLISHED/OFFLINE`、报名 `QUEUING/SUCCESS/FAILED` 等状态变化必须体现在 UI 上，否则联调时会出现“接口成功但用户看不懂”的问题。
- Mock 是 Sprint 1 的加速器，但不是终点；切换到真实 API 时应尽早做，否则 Sprint 2/3 会集中暴露字段、分页、鉴权等契约差异。
- 旁路模块（行为埋点、通知角标等）要先明确“失败是否可接受”，再决定是强一致还是 best-effort；本次行为上报选择后者，与潘度衡后端 best-effort 写入的设计是一致的。
- 后续若继续参与类似项目，我会更早参与 API 契约评审，并在每个 PR 中固定附上验证截图和对应 `SYSTEM_DESIGN.md` 章节，减少后期集成时的反复确认成本；同时会借助 Cursor 等智能体辅助读仓库和生成 Mock，但契约字段、角色权限和状态流转仍坚持人工核对后再合并。

### 5.8 钟志昊

在项目中承担的角色：

- Sprint 2 后端一组成员；Sprint 3 第二组成员。
- 在后端异步处理链路中引入 Redis 与 Kafka：使用 Redis 承担热点数据缓存与请求削峰，使用 Kafka 解耦订单创建流程，提升高并发场景下的吞吐能力和系统稳定性。
- 编写并完善 JMeter 压力测试脚本，对核心下单链路进行并发压测，验证 Redis + Kafka 改造后的 QPS 提升效果和订单冲突修复结果。
- 接入 Prometheus 指标监控，并配置 Grafana 可视化面板，用于观察接口 QPS、响应时间、系统资源与服务运行状态，为后续性能分析和验收演示提供数据支撑。

遇到的主要挑战：

- 高并发下单场景中，传统同步处理链路容易在短时间内承受大量请求，导致接口响应时间上升、数据库压力增大，并可能出现订单冲突或库存状态不一致等问题。
- 压力测试不仅需要能模拟足够真实的并发请求，还要保证测试数据、订单编号和用户参数之间互不冲突，否则压测结果会被业务错误干扰，难以准确反映系统性能。
- 监控体系搭建初期，需要把后端服务指标、压测过程和可视化面板串联起来，才能从数据上判断优化是否真正有效，而不是只依赖接口是否成功返回。

采取的解决方法：

- 通过 Redis 缓存和缓冲热点请求，减少高频访问对数据库的直接冲击；同时引入 Kafka 将下单请求与后续订单处理解耦，使系统可以按消息队列节奏平滑消费请求。
- 针对压测脚本中的订单冲突问题，调整 JMeter 参数化方式和测试数据生成逻辑，使并发请求能够覆盖真实场景，同时避免因为重复数据导致异常结果。
- 接入 Prometheus 采集服务运行指标，并使用 Grafana 构建可视化面板，将 QPS、响应时间、错误率和资源状态等指标集中展示，辅助定位性能瓶颈和验证优化效果。

最终收获：

- 对 Redis 缓存、Kafka 消息队列和后端异步处理链路有了更系统的理解，认识到高并发优化不仅是提高接口速度，更重要的是控制系统各环节的压力传递。
- 掌握了使用 JMeter 进行接口压测的方法，能够通过压测脚本、并发参数和测试数据设计来验证系统在不同负载下的表现。
- 熟悉了 Prometheus 与 Grafana 在项目监控中的基本使用方式，能够通过监控数据观察系统运行状态，并将性能优化结果以更直观的方式展示出来。

深度反思：

- 本次工作让我意识到，性能优化不能只关注单个技术点的引入，而需要从业务流程、数据一致性、请求峰值、失败重试和可观测性等多个角度整体考虑。
- 在之后的开发中，应该更早地设计压测方案和监控指标，让功能开发、性能验证和问题定位形成闭环，避免等到系统接近验收时才集中暴露性能和稳定性问题。
- 同时也需要加强对异步链路异常处理的关注，例如消息消费失败、重复消费和补偿机制等场景，这些问题在普通功能测试中不一定明显，但会直接影响真实高并发环境下的可靠性。

### 5.9 林滨

在项目中承担的角色：

- Sprint 2 前端组成员；Sprint 3 第三组成员；Sprint 5 安全测试负责人。

自己提交代码实现了什么，在项目中的位置：

- 我的工作覆盖前端核心页面开发和后端安全测试两个方向，前端代码位于 `frontend/src/pages`、`frontend/src/components`、`frontend/src/api`、`frontend/src/utils`，安全测试代码位于 `backend/tests` 和 `docs/SECURITY_TEST.md`。
- Sprint 2 阶段完成了登录页面全流程（`Login.tsx`、`auth.ts`、`AuthContext.tsx`、`ProtectedRoute.tsx`），包括 JWT token 管理、Axios 请求拦截、401 自动跳转和登录态持久化；完成了商户端页面体系（`MerchantActivities.tsx`、`MerchantActivityEdit.tsx`、`MerchantActivityNew.tsx`、`MerchantDashboard.tsx`、`MerchantForm.tsx` 及 `MerchantNotice`、`MerchantPageHeader`、`MerchantStateCard` 等组件），支持活动创建、编辑、发布和预热操作；完成了活动详情页优化（`ActivityDetail.tsx`、`ActivityCountdown.tsx`），增加了活动倒计时和状态展示；处理了分支合并后的页面样式和路由问题，涉及 `Activities.tsx`、`Dashboard.tsx`、`Notifications.tsx`、`Orders.tsx`、`Profile.tsx`、`Settings.tsx` 等多个页面的修复。
- Sprint 3 阶段完成了前端交互闭环，包括报名状态页取消报名功能（`EnrollStatus.tsx`）、订单详情与订单列表增强（`OrderDetail.tsx`、`Orders.tsx`）、公开活动列表页（`PublicActivities.tsx`）、行为埋点 API 封装与测试（`behaviors.ts`、`behaviors.test.ts`）、通知未读数 Hook（`useNotificationCount.ts`）、推荐列表和搜索结果的行为上报（`RecommendationList.tsx`、`SearchResultItem.tsx`、`SpotlightActivity.tsx`、`ActivityGridCard.tsx`）以及 Vitest 测试基础设施搭建（`test/setup.ts`、`vitest.config.ts`）和中英文国际化补充。
- Sprint 5 阶段完成了安全授权测试（`security_authz_test.go`，20 个测试用例，覆盖 401 未认证、403 角色权限、水平越权、SQL 注入、XSS、信息泄露和 CORS）、模糊测试（`security_fuzz_test.go`，10 个核心 API × 27 种异常输入）、安全测试报告（`SECURITY_TEST.md`，记录 17 项发现及改进建议），并升级了前后端存在安全风险的依赖（`x/net`、`x/crypto`、`axios`、`react-router-dom`、`vite`）。

遇到的主要挑战：

- 登录流程涉及多个模块联动（Axios 拦截器、AuthContext、ProtectedRoute、token 存储），任何一环出错都会导致登录态丢失或无限跳转，调试链路较长。
- 分支合并后多个页面的样式和路由同时出现问题，需要逐页排查冲突原因，而不是简单地覆盖代码。
- Sprint 3 的前端交互闭环需要同时对接报名、订单、通知、行为、推荐等多个后端接口，接口契约和分页结构不完全一致，联调时需要逐个确认。
- Sprint 5 转到安全测试时，需要从前端开发视角切换到攻击者视角，理解越权、注入、模糊测试等安全概念，并用 Go Test 框架编写可自动化运行的测试用例。

采取的解决方法：

- 登录流程按"token 获取 → 存储 → 拦截器注入 → 401 清理 → 跳转"的顺序逐步实现和验证，每一步单独测试通过后再串联下一步。
- 合并冲突采用逐文件对比策略，先恢复页面能渲染，再修复交互和样式细节，避免一次性改动过多导致新问题。
- 联调时以 `docs/SYSTEM_DESIGN.md` 中的接口契约为基准，前端 API 封装层统一处理分页结构和错误码差异，降低页面层的适配成本。
- 安全测试先阅读 OWASP Top 10 和项目已有的鉴权中间件实现，再按"未认证、角色越权、水平越权、注入、信息泄露"分类设计测试矩阵，模糊测试通过构造异常 payload 批量覆盖核心接口。

最终收获：

- 完整经历了从页面搭建、组件封装、接口对接、国际化到测试补充的前端开发全流程，理解了前端工程不只是写页面，还包括状态管理、鉴权流程、错误处理和测试覆盖。
- 通过商户端和用户端两套页面的开发，理解了角色权限如何在前端路由和 UI 层面落地，以及前后端权限校验需要双重保障。
- 通过安全测试实践，学会了从防御者和攻击者两个视角审视系统，理解了认证、鉴权、输入校验和依赖安全在工程中的实际落地方式。
- 掌握了用 Go Test 编写安全授权测试和模糊测试的方法，能够将安全检查纳入自动化测试流程，而不是只依赖手动验证。

深度反思：

- 前端开发容易只关注"页面能不能用"，但登录态管理、token 安全存储、401/403 处理这些看不见的细节，才是决定系统是否可靠的关键。如果一开始没有设计清楚鉴权流程，后期修补的成本远高于前期投入。
- 安全测试不应该留到最后一个 Sprint 才补充。如果在每个模块开发完成时就同步编写基础的权限和输入校验测试，验收前的压力会小很多，发现问题的时机也更早。
- 从前端开发切换到安全测试，最大的收获是视角转换：写代码时想的是"怎么让功能跑通"，做安全测试时想的是"怎么让功能出错"。这种对抗性思维对提升代码质量很有帮助，未来开发时会更主动地考虑异常输入和权限边界。
- 合并冲突和联调问题占用了不少时间，根本原因是前后端接口契约和分支管理规范在早期没有完全统一。后续项目应该在第一个 Sprint 就固定接口格式、分页结构和分支合并流程，减少后期返工。

### 5.10 宋嘉民

在项目中承担的角色：

- Sprint 4 全链路体验走查、万级数据承载验证与验收收口负责人。
- Sprint 5 验收与实机演示负责人之一，主要负责把系统在真实机器上跑起来、走通核心业务流程，并解释压测和监控结果。
- Sprint 6 参与最终总结材料中项目运行效果、开发流程、验收准备和个人反思部分的整理。

自己提交代码实现了什么，在项目中的位置：

- 在 `backend/scripts/seed/main.go` 中参与 Sprint 4 万级数据构建工作。这个脚本不只是简单插入几条测试数据，而是构建了固定测试账号、批量用户、不同状态的活动、报名记录、订单和用户行为数据，使系统能够在接近真实业务数据量的情况下运行和展示。Seeder 里使用批量插入和 `OnConflict DoNothing`，可以重复执行，减少了验收前清库、重跑数据时的不确定性。
- 在 `backend/internal/infra/kafka.go` 中参与修复 Kafka 消费链路问题。之前压测和手工报名时出现过“接口返回排队成功，但订单迟迟不生成，报名一直停在 `QUEUING`”的现象。最后定位到 Kafka consumer 在新消费者组场景下可能从 topic 尾部开始读，导致历史消息被跳过。通过设置 `StartOffset: kafka.FirstOffset`，让没有提交 offset 的消费者从最早消息开始消费，使报名 `QUEUING -> SUCCESS -> 订单生成` 的异步链路恢复闭环。
- 在 `backend/tests/jmeter/run-jmeter-report.sh` 和 Sprint4 压测材料中参与万级压测流程整理。脚本支持不同线程规模，压测前会登录商户、创建并发布活动、准备用户 token CSV，再执行 JMeter headless 测试并生成 HTML 报告。这里我也明确了一个很重要的口径：`10000` 代表 10000 个独立用户/线程各发起一次报名请求，不是 10000 QPS；实际吞吐要结合 ramp-up 时间和 JMeter 报告解释。
- 在 `docs/SPRINT/SPRINT4.md` 中整理 Sprint 4 完成记录，记录万级数据规模、JMeter 10000 请求压测结果、Kafka 问题修复、前端关闭 Mock 后的真实联调走查结果。这个文档后来成为 Sprint6 最终总结和验收说明的重要依据。
- 在前端环境方面参与处理 pnpm 和依赖版本问题，包括 `frontend/pnpm-lock.yaml`、`frontend/pnpm-workspace.yaml` 相关调整。这个问题本身不是业务功能，但它影响团队成员和验收机器能否稳定安装依赖、启动前端。我也因此认识到工具链版本、lockfile 和运行说明同样属于项目交付的一部分。

遇到的主要挑战：

- 最大的挑战不是单独写某一个接口，而是项目后期系统已经包含前端、后端、MySQL、Redis、Kafka、Prometheus、Grafana、JMeter 等多个部分，任何一个环节配置不对，演示就会失败。比如 Docker 容器状态、后端 `.env`、前端是否关闭 Mock、Redis 库存是否预热、Kafka Worker 是否消费，都会影响最终效果。
- 第二个挑战是异步链路不容易从表面判断对错。报名接口返回 `202` 只说明请求进入排队，并不代表订单已经生成。真正完整的链路是：前端点击报名 -> 后端鉴权 -> Redis Lua 原子扣库存和防重复 -> MySQL 创建 `QUEUING` 报名 -> Kafka 投递消息 -> Worker 消费 -> 报名变为 `SUCCESS` -> 创建订单和通知。如果只看接口返回，很容易误以为系统已经成功。
- 第三个挑战是压测结果需要讲清楚。JMeter 报告里有总请求数、错误率、吞吐量、P90/P95/P99 等指标；Grafana 里又有 HTTP 吞吐、状态码、Worker success/failure、Kafka lag。刚开始我也会把“线程数”“请求总数”“QPS”混在一起，后来才意识到验收时必须把这些指标和业务链路对应起来，否则老师只看到一堆数字，不知道系统到底证明了什么。
- 第四个挑战是环境一致性。比如 pnpm 版本差异、Docker 端口冲突、MySQL 数据卷残留、JMeter 本机线程限制、前端 Mock 开关等问题，都不是代码逻辑 bug，但都会影响最终验收。项目到后期才集中处理这些问题，压力会比较大。

采取的解决方法：

- 我采用的是从“能否真实跑通”倒推的方式，而不是只看代码。先确认 Docker 服务、后端健康检查、数据库连接、前端真实 API、Seed 数据，再走商户创建活动、发布活动、用户报名、订单生成、通知展示，最后再上 JMeter 和 Grafana。
- 对异步报名链路，我没有只停留在前端页面，而是结合数据库状态、Redis 库存、Kafka Worker 日志和 Grafana 指标一起判断。特别是 `QUEUING`、`SUCCESS`、订单 `PENDING/CLOSED`、Redis stock、Worker success、Kafka lag 这些状态，必须放在同一条业务链路里理解。
- 对万级数据和压测，我把测试数据、压测参数和结论写进文档。比如活动容量设置为线程数的 5 倍，是为了避免压测主要测到“售罄”分支；10000 用户压测关注的是系统能否稳定接收请求、避免超卖、避免重复成功，并让 Worker 最终消费完成。
- 对环境问题，我尽量把解决过程沉淀为文档和可复用命令。因为验收现场最怕“只有某个人电脑能跑”，所以运行步骤、依赖版本、健康检查、常见故障排查都需要提前准备。

最终收获：

- 我最大的收获是理解了“大规模信息系统”不只是把 CRUD 页面和接口写完。真正到验收阶段，一个系统必须能被启动、能被复现、能承载一定数据量、能解释并发行为、能通过监控证明自己没有出错。
- 我对 Redis、Kafka、JMeter、Prometheus 和 Grafana 的理解比项目开始时具体很多。以前我可能只知道 Redis 是缓存、Kafka 是消息队列；这次真正看到它们在抢票链路里的作用：Redis 负责高并发下的原子库存扣减，Kafka 负责削峰，Worker 负责异步落库，Prometheus/Grafana 负责把系统运行状态变成可观察的数据。
- 我也认识到文档不是最后随便补的材料。像 Sprint4 完成记录、压测报告、运行指南、监控说明这些文档，实际上是在帮助团队确认“我们到底完成了什么、怎么证明它完成了、别人能不能复现”。如果没有这些材料，项目即使代码能跑，也很难在验收时讲清楚。
- 在使用智能体辅助开发和整理材料时，我的收获是：智能体很适合帮忙跨文件梳理、定位问题、整理演示流程，但不能直接替代人工判断。尤其是涉及压测数字、代码路径、真实运行结果时，必须回到仓库、日志、命令输出和截图证据中核实。

深度反思：

- 这次项目后期我承担的是收口和验收角色，因此感受很明显：前期每个模块局部完成，不等于最终系统就自然稳定。真正困难的是把所有模块串起来，并且在真实数据、真实前后端、真实中间件下验证它。
- 如果重新做一次，我会更早推动团队建立统一的运行标准，比如固定 pnpm 版本、固定 Docker 启动方式、固定 Seed 数据、固定压测命令、固定验收检查清单。这样很多环境问题不会集中爆发在最后。
- 我也会更早要求每个核心功能都给出“如何证明它正确”的方法。比如报名功能不能只说接口写完了，还要证明不超卖、不重复成功、Worker 能消费、订单能生成、通知能触达；推荐功能不能只说有接口，还要说明冷启动、热门兜底和用户行为如何影响结果。
- 对我个人来说，这个项目让我意识到工程能力不只是写代码，还包括定位问题、解释系统、整理证据和控制风险。尤其是验收演示时，老师不一定会看每一行代码，但会通过系统是否稳定、数据是否一致、指标是否合理来判断我们是否真正理解了这个项目。
- 所以我觉得自己在 Sprint4 和 Sprint5 的价值，不只是补了某几个文件，而是把项目从“多个模块已经实现”推进到“可以在真实环境中运行、压测、观察和讲清楚”的状态。这也是我在这门课里最重要的收获。

### 5.11 李浩博

在项目中承担的角色：

- 我是全组组长，负责划定项目范围、拆解阶段目标、构建各轮 Sprint、把控项目整体进度，并推动团队在功能开发、测试验证、验收准备和总结反思之间形成闭环。
- 从 Sprint 1 到 Sprint 6，我持续承担项目组织和方向把控工作：前期明确需求范围和前后端分工，中期根据开发进展调整后端、前端、联调和测试分组，后期围绕安全测试、实机演示、抗压展示和最终课程报告进行收口。
- 我也负责构建和维护项目中的大部分核心文档，包括需求范围、Sprint 计划、协作安排、验收准备、总结反思等内容，使团队成员能够围绕统一目标推进，而不是各自零散开发。
- 在 Sprint 6 中，我除了承担组长视角的总复盘外，也负责微服务架构专题报告，说明当前项目未完全微服务化的原因、潜在拆分策略、服务间通信机制和推荐技术路径。

自己提交代码实现了什么，在项目中的位置：

- 我的主要贡献集中在项目范围定义、Sprint 规划、团队协作组织和文档建设上，而不是某一个单独业务模块的代码实现。
- 在项目早期，我负责把课程要求和团队能力转化为可执行的项目范围，明确 UAAD 要完成活动发布、活动报名、订单支付、通知、推荐、压测、监控、安全测试和最终验收等核心目标，避免范围过大或方向发散。
- 在 Sprint 组织上，我负责构建每一轮 Sprint 的工作安排：Sprint 1 划分后端 1、后端 2、前端组；Sprint 2 根据进展重组后端一组、后端二组和前端；Sprint 3 重新按功能闭环分三组推进；Sprint 4 聚焦全链路体验和万级数据承载；Sprint 5 转向安全测试、实机演示和抗压能力展示；Sprint 6 聚焦最终总结、反思和课程报告。
- 在文档层面，我主导或推动了项目大部分文档建设，包括 Sprint 规划、协作规则、运行说明、系统总结、测试与验收材料、课程反思和最终报告结构。这些文档是团队沟通、任务分配和验收复现的重要依据。
- 在 Sprint 6 中，我补充微服务架构专题，内容位于 `docs/SPRINT6.md` 的 `8.2 微服务架构` 小节。我围绕当前项目实际结构进行分析，说明后端虽然仍是单体部署，但内部已经按 `handler / service / repository / worker` 分层，并形成了用户认证、活动、报名、订单、通知、推荐、行为埋点等较清晰的业务边界。

遇到的主要挑战：

- 作为组长，最大的挑战是项目范围控制。课程要求覆盖功能实现、智能体使用、测试报告、压力测试、监控、安全性、弹性设计、微服务思考和最终反思，如果不主动划定边界，团队很容易陷入“什么都想做但都做不完整”的状态。
- 第二个挑战是进度把控。不同成员熟悉的技术栈和推进速度不同，前后端、测试、文档和演示之间也存在依赖关系，需要不断根据实际进展调整 Sprint 分工。
- 第三个挑战是协调代码实现和文档建设。很多项目事实会随着开发变化，如果文档不及时更新，后续验收和总结时就会出现“文档说法”和“代码实现”不一致的问题。
- 第四个挑战是使用智能体辅助开发时的质量控制。智能体可以快速生成方案、代码和文档，但也可能误解项目事实，因此需要人工判断哪些内容可以采纳、哪些需要重写。
- 在微服务专题上，挑战是项目当前没有真正拆成多个微服务，因此不能夸大实现，只能基于现有模块边界和 Kafka 异步链路进行合理分析。

采取的解决方法：

- 我通过 Sprint 机制把大目标拆成阶段性目标。每一轮 Sprint 都明确当前重点，例如早期先搭建基础功能，中期补齐业务闭环，后期集中处理压测、安全、实机演示和总结报告。
- 我根据团队实际进展动态调整人员安排，而不是固定不变地沿用最初分组。这样可以让后端、前端、测试和演示任务在不同阶段获得更合适的人力支持。
- 我持续维护文档，把口头安排沉淀为可追溯的 Sprint 计划、协作说明、运行指南和总结材料，减少团队成员之间的信息差。
- 对智能体生成的内容，我采用“先读项目事实，再改写成团队真实情况”的方式使用，避免直接照搬泛化模板。尤其是在 Sprint6 总结中，需要区分哪些内容是全组汇报、哪些内容必须每个人分别撰写。
- 对微服务架构专题，我从当前系统的真实模块出发，说明当前保持模块化单体更稳，同时提出“先拆异步、再拆旁路、最后拆核心交易链路”的演进路线。

最终收获：

- 我最大的收获是对项目管理和工程协作有了更真实的理解。一个项目能不能最终完成，不只取决于某个功能是否写出来，还取决于范围是否清晰、任务是否拆得合理、进度是否可控、文档是否跟得上、验收是否能复现。
- 我学会了如何把课程要求转化为团队可执行的 Sprint：先明确目标，再拆分角色，再根据阶段进展调整分工，最后用文档和报告把结果沉淀下来。
- 我认识到组长需要同时关注技术和协作。技术上要理解系统架构、核心链路、测试和风险；协作上要保证每个人知道自己该做什么、为什么做、做到什么程度算完成。
- 在智能体使用方面，我体会到智能体非常适合辅助生成文档结构、梳理代码和总结问题，但组长必须负责最终判断，不能让智能体替代团队对项目事实的理解。
- 在微服务架构专题中，我对“模块化单体”和“微服务演进”的关系有了更实际的理解：不一定一开始就要拆服务，但必须保留清晰的业务边界，为未来演进留空间。

深度反思：

- 回头看整个项目，我认为前期范围划定非常关键。如果一开始目标过散，后面每个 Sprint 都会变成补漏洞；如果目标过窄，又无法满足课程对压测、安全、监控、智能体和总结反思的要求。组长最重要的工作之一，就是在“做得完整”和“做得可控”之间找到平衡。
- 我也反思到，文档不应该只在最后补。项目过程中很多安排、取舍和问题如果没有及时记录，最后总结时就很难还原真实过程。后续类似项目中，我会要求每个 Sprint 结束时固定更新任务完成情况、问题记录和下一轮调整原因。
- 在团队协作中，我还需要更早明确“谁负责实现、谁负责测试、谁负责演示、谁负责报告”。如果责任边界不够清晰，后期容易出现某些工作被默认推给组长或临近验收才集中补齐。
- 对智能体的使用也需要更有边界。它能帮助我快速整理大量文档和代码信息，但不能替代团队成员自己的反思，也不能自动知道真实分工。之后我会更早把真实分工、真实贡献和团队约束告诉智能体，减少后期纠偏成本。
- 对架构层面，我认为本项目没有强行拆微服务是正确的。工程判断不是追求概念先进，而是选择适合当前团队、周期和业务复杂度的方案。当前模块化单体加 Kafka 异步解耦已经足够支撑。

---

## 六、缺陷与反思

### 6.1 已发现并处理的问题

| 问题 | 表现 | 处理方式 | 反思 |
|---|---|---|---|
| 数据库连接失败 | 后端启动时报 MySQL 连接拒绝或 root 权限错误 | 检查 Docker MySQL、端口映射、本地 3306 占用和 `.env` 配置 | 运行依赖应尽早脚本化和文档化 |
| 前端 pnpm 启动失败 | `packages field missing or empty` | 修复 `frontend/pnpm-workspace.yaml` | 工具链配置也属于验收风险 |
| 报名可能长期卡在 `QUEUING` | Kafka 异步写入可能导致接口先返回成功但消息未实际投递 | 将 Kafka Writer 调整为同步写入并设置超时 | 异步系统不能只看接口返回，还要关注消息可靠性 |
| 活动预热状态原先只有概念 | `PREHEAT` 没有真实设置入口 | 新增后端预热接口和前端商户操作 | 状态机中的每个状态都应有明确进入路径 |
| 安全测试初期覆盖不足 | 验收前才集中补安全与模糊测试 | Sprint 5 补充安全授权测试和模糊测试 | 安全测试应更早进入迭代节奏 |
| 压测链路理解成本高 | JMeter、Grafana、Prometheus、Kafka 指标较多 | 在文档中固定压测命令、指标解释和演示路径 | 抗压能力展示要面向评审可理解，而不只是能跑脚本 |

### 6.2 使用智能体过程中的心得体会

正向作用：

- 智能体能快速读取仓库、定位文件、总结功能和整理文档。
- 对错误日志的解释和排查路径很有帮助，尤其是 Docker、MySQL、Kafka、pnpm 等问题。
- 对跨模块变更有帮助，例如活动预热同时涉及后端 service、handler、routes、前端 API、页面操作和文档。
- 对测试补充有帮助，可以根据验收口径生成安全测试、模糊测试、集成测试和运行命令。
- 对最终材料整理很有帮助，可以把分散的文档、测试、报告和运行说明汇总为提交结构。

暴露的问题：

- 智能体可能根据一般经验给出建议，但项目真实代码不一定一致，因此必须要求其先读仓库。
- 智能体容易生成“看起来完整”的文档，但其中的人名、日期、测试结果、截图证据必须由团队核实。
- 智能体不能代替团队理解业务状态机和系统边界，尤其是报名、库存、订单、支付、取消这些关键链路。
- 智能体生成的代码仍需要测试验证，不能只靠文字解释判断正确。

改进经验：

- 提问时给出当前错误日志、所在目录、运行命令和期望结果。
- 让智能体先查文件再回答，减少幻觉。
- 对每次变更要求给出验证命令。
- 对验收材料要求区分“已实现”“已测试”“已记录风险”和“未来建议”。
- 对个人心得、课程反馈等主观材料，由成员自己补充真实经历，智能体只负责结构化。

### 6.3 团队协作反思

本项目最大收获之一是理解到：系统开发不是把功能分别写完就结束，而是要让功能、测试、运行、监控、文档和演示共同闭环。

反思点：

- 如果没有统一运行指南，系统即使能在某个人电脑上跑，也不代表能在验收现场稳定运行。
- 如果没有测试和监控，高并发报名这类功能很难证明“真的可靠”。
- 如果没有明确分工，后期容易出现大家都以为别人会补文档、补测试、补演示脚本的情况。
- 如果文档没有持续维护，最终总结阶段会花大量时间重新追溯项目事实。
- 如果每个成员只了解自己负责的局部，实机演示时很难应对临时问题。

后续类似项目应在一开始就建立：

- 固定任务看板。
- 每周可运行版本。
- 每个 Sprint 的 DoD。
- 最小测试要求。
- 文档入口和材料命名规范。
- 演示脚本和故障排查清单。

---

## 七、课程建议

### 7.1 对课程内容的建议

建议增加以下内容：

- 增加“从需求到可运行系统”的完整样例，展示如何从 SRS、架构设计、API、数据库、测试、部署一步步推进。
- 增加 AI Agent 辅助开发的边界说明，特别是如何避免智能体幻觉、如何审查智能体生成代码、如何记录智能体交互过程。
- 增加中间件实践讲解，例如 Redis、Kafka、Prometheus、Grafana 在一个小型系统中分别解决什么问题。
- 增加安全测试基础内容，包括认证、鉴权、越权、XSS、SQL 注入、模糊测试和依赖漏洞扫描。
- 增加压测结果解释方法，不只教如何运行工具，还要教如何看 P95、P99、错误率、吞吐量和资源瓶颈。

### 7.2 对组织形式的建议

建议课程组织上：

- 每个阶段明确必交材料，例如需求文档、系统设计、测试报告、运行指南、复盘报告。
- 每个 Sprint 增加一次短检查，确认系统是否能从零拉起。
- 让团队尽早确定角色分工，但允许根据实际进展调整。
- 要求每个成员至少完成一次代码讲解或演示讲解，避免少数成员承担全部理解成本。
- 验收前增加一次模拟验收，提前暴露环境、脚本、数据和讲解问题。

### 7.3 对教学方法的建议

建议教学方法上：

- 多给“可复现”的评价标准，例如必须提供运行命令、测试命令、截图或日志。
- 对 AI 辅助开发过程设置记录要求，例如保留关键对话、关键 prompt、智能体生成内容和人工修改说明。
- 鼓励学生使用真实工程工具，如 Docker、CI、测试框架、监控系统，而不仅是完成页面和接口。
- 对复杂系统设计引入阶段性评审，帮助学生尽早发现架构过度设计或实现不足的问题。
- 对最终报告提供模板，但保留团队自由发挥空间。

---

## 八、五个技术维度总结

### 8.1 压力测试

已完成探索：

- 使用 Go benchmark 验证报名吞吐和活动列表吞吐。
- 使用 Go 并发测试验证小库存高并发下不会超卖。
- 使用 JMeter 脚本对报名接口进行压力测试。
- 使用 Prometheus 和 Grafana 观察压测期间的 HTTP 指标、Worker 指标和 Kafka 消费情况。

相关文件：

- `backend/tests/stress_test.go`
- `backend/tests/jmeter/enrollment-load.jmx`
- `backend/tests/jmeter/run-jmeter-report.sh`
- `backend/scripts/gen_jmeter_data/main.go`
- `docs/STRESS_TEST/ST_BASELINE.md`
- `docs/STRESS_TEST/ST_REPORT.md`
- `docs/STRESS_TEST/1000t_st/`
- `docs/STRESS_TEST/3000t_st/`
- `docs/STRESS_TEST/5000t_st/`

测试方法：

```powershell
cd backend
go test -v -tags=stress -bench=. -benchtime=10s -count=1 ./tests/
```

```powershell
cd backend/tests/jmeter
bash run-jmeter-report.sh
```

关注指标：

- 总请求数。
- 成功率与错误率。
- P90 / P95 / P99 响应时间。
- 每秒吞吐量。
- 库存一致性。
- Kafka lag。
- Worker success / failure。

实际表现总结：

- 系统具备通过 Redis 原子扣减和 Kafka 异步处理承接并发报名的能力。
- 压测过程中需要区分“业务性失败”和“系统性失败”。例如库存售罄属于业务预期，不应简单计为系统错误。
- Grafana 能帮助解释压测期间请求量、延迟、错误和 Worker 处理情况。

优化方向：

- 增加更多压测场景，例如活动列表、推荐接口、订单支付、通知查询。
- 固定不同机器规格下的压测基线。
- 增加压测前自动清理数据和压测后自动一致性校验。
- 将 JMeter 报告和 Grafana 截图按日期归档。

### 8.2 微服务架构

当前实践情况：

- 当前系统仍以单体后端为主，没有完全拆分为多个独立微服务。
- 这是符合当前项目阶段的选择：课程项目更需要先保证核心业务闭环、可运行性、测试和验收稳定性，过早拆成多个服务会显著增加部署、联调、事务一致性和故障排查成本。
- 虽然没有完整微服务化，但系统内部已经具备一定的服务化边界，包括用户认证、活动、报名、订单、通知、推荐、行为埋点和 Worker 等模块。
- 后端代码按 `handler / service / repository / worker` 组织，业务职责相对清晰，为后续服务拆分提供了结构基础。
- Kafka Worker 已经体现了异步服务化思路：报名请求与后端落库、订单创建被消息队列解耦。报名接口负责快速准入，Worker 负责异步完成较重的数据库事务。

当前不完全微服务化的原因：

- 项目规模仍适合模块化单体，单体部署更易启动、调试和验收。
- 多服务拆分会带来额外的网关、服务发现、配置中心、日志聚合、链路追踪和部署编排成本。
- 报名、库存、订单、通知之间存在较强一致性关系，过早拆分会引入分布式事务和补偿逻辑，增加出错概率。
- 团队在课程周期内的重点是完成功能闭环、压测、安全测试和演示。

潜在拆分策略：

| 服务 | 职责 | 数据/通信 |
|---|---|---|
| 用户认证服务 | 注册、登录、JWT、用户资料 | MySQL、HTTP |
| 活动服务 | 活动创建、发布、预热、下架、查询 | MySQL、Redis |
| 报名服务 | 报名请求、库存扣减、幂等控制 | Redis、Kafka、MySQL |
| 订单服务 | 订单生成、支付、过期扫描 | MySQL、消息事件 |
| 通知服务 | 通知创建、未读数、已读状态 | MySQL、消息事件 |
| 推荐服务 | 行为收集、热度计算、推荐生成 | MySQL、缓存 |
| 监控服务 | 指标采集、日志聚合、告警 | Prometheus、Grafana |

服务间通信机制建议：

- 同步查询类场景优先使用 HTTP REST 或 gRPC，例如活动详情、订单详情、用户资料查询。
- 异步事件类场景优先使用 Kafka，例如报名成功、报名失败、订单过期、通知生成、推荐热度更新。
- 对强一致要求高的链路，例如库存扣减和订单创建，需要谨慎拆分，优先通过本地事务、状态机、幂等键和补偿机制保证最终一致性。
- 服务之间应统一 trace id 或 request id，便于在拆分后进行链路追踪和问题定位。

推荐技术路径：

- 第一阶段：保持模块化单体，明确 service、repository、handler 边界。
- 第二阶段：将 Worker 独立进程化，报名 API 与消费处理分离部署。该阶段改动最小，因为当前系统已经通过 Kafka 将两者解耦。
- 第三阶段：优先拆出推荐服务和通知服务。这两个模块对主链路影响较小，失败时可以降级或重试，适合作为第一批独立服务。
- 第四阶段：根据业务压力再考虑拆分用户认证、活动、报名和订单服务，同时引入 API Gateway、服务发现、集中配置、链路追踪和统一日志。
- 第五阶段：如果进入生产化部署，再考虑容器编排、弹性扩缩容、灰度发布和服务级告警。

收益：

- 模块独立部署。
- 不同服务可按负载独立扩容。
- 推荐、通知、订单等模块可独立演进。
- 高并发报名链路可以单独优化。

风险：

- 分布式事务复杂度增加。
- 部署和监控成本提升。
- 本课程项目规模下完全微服务化可能超过实际需要。
- 服务调用链路变长，故障定位难度上升。
- 网络调用失败、消息重复消费、服务版本不兼容等问题需要额外治理。

结论：

- UAAD 当前阶段更适合“模块化单体 + 局部异步解耦”的架构。
- Kafka Worker 已经为后续微服务化提供了一个自然切入点。
- 后续如果继续演进，应遵循“先拆异步、再拆旁路、最后拆核心交易链路”的顺序，避免为了微服务而微服务。

### 8.3 系统监控

已部署方案：

- Prometheus 采集后端指标。
- Grafana 展示系统监控 Dashboard。
- 后端暴露 `/metrics`。
- Docker Compose 管理 Prometheus 与 Grafana。

相关文件：

- `infra/grafana/provisioning/dashboards/uaad-sprint3.json`
- `docs/Prometheus_And_Grafna.md`
- `docker-compose.yaml`

主要面板：

- HTTP 吞吐。
- HTTP 状态码分布。
- 总请求量。
- 5xx 错误数。
- HTTP P50 / P95 / P99。
- 按 Path 的 P95。
- Worker 处理吞吐。
- Worker 耗时 P95。
- Kafka 消费滞后。
- Worker 处理消息数。

实际成效：

- 在压测和演示中可以直观看到请求量上升。
- 可以通过状态码分布判断是否出现大量非预期错误。
- 可以通过 Worker 指标判断 Kafka 消息是否被正常消费。
- 可以通过 Kafka lag 判断异步处理是否堆积。

改进建议：

- 增加 MySQL、Redis、Kafka 自身指标采集。
- 增加日志聚合，例如 Loki 或 ELK。
- 增加告警规则，例如 5xx 过高、P95 过高、Kafka lag 过高。
- 区分业务错误和系统错误的监控口径。
- 生产环境下对 `/metrics` 做网络隔离或认证保护。

### 8.4 弹性设计

当前已采取的措施：

- Redis 原子扣减库存，降低数据库直接承压。实现位于 `backend/internal/service/stock_lua.go`，Lua 脚本一次性完成重复报名判断、库存检查、扣减、写入已报名集合和生成排队序号。
- Kafka 异步削峰，避免报名请求直接把数据库打满。接口层在 Redis 准入后先创建 `QUEUING` 报名记录并写入 Kafka，Worker 再异步完成订单创建。
- Worker 异步处理报名消息。实现位于 `backend/internal/worker/enrollment_worker.go`，Worker 在事务中推进报名状态、创建订单并更新活动报名计数。
- 报名幂等控制，降低重复请求风险。Redis 已报名集合防止入口重复报名，数据库层通过 `QUEUING`、`PENDING` 等状态条件更新防止重复消费和重复状态流转。
- 订单过期扫描和库存回滚，避免资源长期占用。实现位于 `backend/internal/service/order_service.go`，`ScanExpired` 只关闭仍处于 `PENDING` 的过期订单，并回补 MySQL 与 Redis 库存。
- Kafka 同步写入和超时设置，降低消息丢失导致状态卡住的风险。消息写入失败时会将 Redis 扣减补偿回去并把报名标记为失败。
- 健康检查、Prometheus 指标和 Grafana 面板辅助发现运行问题，重点观察 HTTP 延迟、错误率、Worker 处理量和 Kafka lag。

核心链路说明：

- 用户提交报名后，服务先校验活动状态和报名窗口，再调用 Redis Lua 脚本完成原子准入。如果返回重复报名或库存不足，接口直接返回业务错误，不进入数据库写入和 Kafka 阶段。
- Redis 准入成功后，系统写入 `QUEUING` 报名记录并投递 Kafka 消息。这个阶段把“用户请求是否被接收”和“订单是否最终创建”拆开，从而削平瞬时流量峰值。
- Worker 消费消息后，在一个事务内将报名推进为 `SUCCESS`、创建 15 分钟过期的 `PENDING` 订单，并增加活动报名计数。若事务失败，Worker 会回滚 Redis 库存并把报名标记为 `FAILED`。
- 用户取消或订单过期时，系统通过条件更新抢占状态流转权，只有成功从 `QUEUING`、`SUCCESS` 或 `PENDING` 迁移出的请求才执行库存回补，避免并发取消和过期扫描造成重复回滚。

幂等与补偿设计：

- 入口幂等：Redis `enrolled_set` 记录已经通过准入的用户，重复请求不会再次扣减库存。
- 消费幂等：Worker 只处理当前仍为 `QUEUING` 的报名消息，重复消息或已取消消息会被跳过。
- 订单幂等：支付、过期关闭和取消都依赖 `PENDING` 条件更新，避免同一订单被多条路径重复关闭或支付。
- 库存补偿：`Rollback` 同时执行 Redis 库存加一和已报名集合移除，覆盖 Kafka 投递失败、Worker 事务失败、用户取消和订单过期等路径。
- 最终一致性：Redis 承担实时库存，MySQL 记录最终报名和订单状态；系统通过补偿、状态机和测试验证两端逐步收敛。

相关测试与验证材料：

- `backend/internal/service/stock_lua_test.go` 覆盖 Redis 原子扣减、重复报名和回滚。
- `backend/internal/worker/enrollment_worker_test.go` 覆盖 Worker 成功处理、事务失败回滚和非 `QUEUING` 消息跳过。
- `backend/internal/service/order_service_flow_test.go` 覆盖订单过期关闭、通知和库存回滚。
- `backend/internal/service/race_isolation_test.go` 覆盖取消、支付和过期扫描之间的竞态隔离。
- `backend/tests/sprint3_order_expire_test.go` 和 `backend/tests/sprint3_closed_loop_test.go` 覆盖接近真实依赖的闭环场景。

已有收益：

- 高并发报名链路具备一定抗压能力，热点库存竞争首先落在 Redis 原子脚本上，而不是直接落在 MySQL 行锁或表锁上。
- 库存扣减更加稳定，重复报名和库存不足在入口阶段即可被识别，减少后续无效消息和无效订单。
- 请求处理和落库处理解耦，接口可以快速返回排队状态，Worker 按消费能力逐步完成持久化。
- 异步 Worker 可作为后续独立扩容点，后续可以根据 Kafka lag 或 Worker 耗时指标增加消费者实例。
- 异常路径具备补偿闭环，常见失败场景不会长期占用库存，也不会让订单资源无限期悬挂。

不足：

- 暂未引入断路器。
- 暂未系统性实现重试策略和死信队列。
- 暂未实现服务自动扩容。
- 暂未实现负载均衡部署。


未来可引入的弹性策略：

- 对 Kafka 消费失败引入分级重试和死信队列。短暂失败先延迟重试，多次失败后进入死信主题，并提供后台补偿或人工排查入口。
- 对 Redis、Kafka、MySQL 等关键依赖引入超时、重试和熔断。重试需要限制次数并加入退避，避免故障期间放大流量。
- 对推荐、通知未读数等非核心接口提供降级结果，例如推荐接口异常时返回热门活动，通知角标异常时不阻塞核心报名流程。
- 将 Worker 独立部署并允许多实例水平扩展。
- 使用 Nginx 或 API Gateway 做负载均衡。
- 使用容器编排平台实现自动恢复。
- 增加 Redis/MySQL 库存对账任务，定期扫描报名、订单和 Redis 集合差异，对异常 `QUEUING`、`FAILED` 或库存漂移进行修复。
- 增加核心链路限流和排队保护，例如按活动维度限制报名 QPS，超限时返回可解释的排队或稍后重试提示。

### 8.5 安全性保障

现有安全措施：

- JWT 身份认证。
- bcrypt 密码哈希。
- 角色权限控制。
- 用户私有资源归属校验。
- GORM 参数化查询降低 SQL 注入风险。
- 前端未使用 `dangerouslySetInnerHTML`，降低 XSS 执行风险。
- CORS 配置支持开发和生产区分。
- 安全授权测试覆盖 401、403、越权、非法输入、信息泄露和 CORS。
- 模糊测试覆盖核心 API 的异常输入。
- 依赖安全扫描覆盖 Go 和前端依赖。

相关文件：

- `backend/tests/security_authz_test.go`
- `backend/tests/security_fuzz_test.go`
- `docs/SECURITY_TEST.md`

安全测试运行方式：

```powershell
cd backend
go test -v -tags=bgroup -run '^TestSec|^TestFuzz' -count=1 ./tests/
```

已知不足：

- JWT secret 存在开发默认值，生产环境必须覆盖。
- JWT 存储在 localStorage，仍有 XSS 场景下被窃取的风险。
- 登录接口限流仍需进一步加强。
- `/metrics` 在演示环境中公开，生产环境需隔离。
- 部分异常输入可能返回 500，说明输入校验还可以更前置、更精细。
- 服务端 token 撤销机制尚未实现。

后续加固方向：

- 启动时强制检查生产环境 JWT secret。
- 增加登录限流和验证码策略。
- 对注册、登录、活动创建等接口加强字段格式和长度校验。
- 使用 HttpOnly Cookie 或更严格的 token 存储策略。
- 增加 CSRF 防护设计。
- 增加敏感操作审计日志。
- 对 Prometheus metrics 做认证或内网隔离。
- 引入 OWASP ZAP 等动态扫描工具。

---

## 九、测试与实验报告汇总

### 9.1 单元测试

后端：

```powershell
cd backend
go test ./... -count=1
```

覆盖范围：

- service。
- handler。
- worker。
- middleware。
- config。
- jwtutil。
- response。

前端：

```powershell
cd frontend
pnpm test
```

覆盖范围：

- API 请求封装。
- Axios 鉴权处理。
- 业务错误处理。
- behavior endpoint。
- enrollment endpoint。
- smoke test。

### 9.2 集成测试

```powershell
cd backend
go test -v -tags=integration -count=1 ./tests/
```

覆盖范围：

- 报名闭环。
- 取消报名。
- 订单过期。
- 通知数量。
- 活动过期下架。
- 并发库存一致性。

### 9.3 B 组验收测试

```powershell
cd backend
go test -v -tags=bgroup -count=1 ./tests/
```

覆盖范围：

- BGroup 集成链路。
- JWT 鉴权。
- 响应契约。
- 安全授权。
- 模糊测试。

### 9.4 压力测试

```powershell
cd backend
go test -v -tags=stress -bench=. -benchtime=10s -count=1 ./tests/
```

```powershell
cd backend/tests/jmeter
bash run-jmeter-report.sh
```

报告位置：

- `docs/STRESS_TEST/ST_BASELINE.md`
- `docs/STRESS_TEST/ST_REPORT.md`
- `docs/STRESS_TEST/1000t_st/`
- `docs/STRESS_TEST/3000t_st/`
- `docs/STRESS_TEST/5000t_st/`

### 9.5 安全测试

```powershell
cd backend
go test -v -tags=bgroup -run '^TestSec|^TestFuzz' -count=1 ./tests/
```

报告位置：

- `docs/SECURITY_TEST.md`

### 9.6 构建与静态检查

前端：

```powershell
cd frontend
pnpm lint
pnpm build
pnpm test
```

后端：

```powershell
cd backend
go test ./... -count=1
```

依赖安全：

```powershell
cd backend
govulncheck ./...
```

```powershell
cd frontend
pnpm audit
```

---

## 十、配套材料清单


### 10.1 项目基础文档

| 材料 | 位置 |
|---|---|
| 项目说明 | `README.md` |
| 文档中心 | `docs/README.md` |
| 需求规格说明 | `docs/SRS.md` |
| 系统设计 | `docs/SYSTEM_DESIGN.md` |
| 数据库设计 | `docs/DDL.md` |
| 抢票引擎设计 | `docs/TICKET_ENGINE_DESIGN.md` |
| 推荐系统设计 | `docs/RECOMMENDATION_DESIGN.md` |
| 前端规范 | `frontend/FRONTEND_SPEC.md` |
| 运行指南 | `docs/RUN_GUIDE.md` |

### 10.2 Sprint 与协作材料

| 材料 | 位置 |
|---|---|
| 协作规则 | `docs/COLLABORATION.md` |
| Sprint 记录目录 | `docs/SPRINT/` |
| Sprint 4 记录 | `docs/SPRINT4.md` |
| Sprint 5 记录 | `docs/SPRINT5.md` |
| Sprint 6 最终总结 | `docs/SPRINT6.md` |
| WBS 图 | `docs/wbs.puml` |
| 后端任务清单 | `backend/task.md` |
| 前端任务清单 | `frontend/task.md` |

### 10.3 智能体、规则与 Skills 材料

| 材料 | 位置 |
|---|---|
| 文档与 Prompt 治理规则 | `docs/DOCS_PROMPTS_GOVERNANCE.md` |
| Codex OpenSpec Skills | `.codex/skills/` |
| Agent Skills | `.agents/skills/` |
| Agent Workflows | `.agents/workflows/` |
| OpenSpec 变更 | `openspec/changes/` |
| OpenSpec 稳定规格 | `openspec/specs/` |
| Cursor 规则 | `.cursorrules` |
| Skills 锁定文件 | `skills-lock.json` |

说明：

- 课程要求中的 “MCP Server 设置” 需要根据团队实际使用情况补充。如果项目中存在单独 MCP 配置文件，应一并提交。
- 当前仓库可见的是多智能体配置目录、Skills、OpenSpec 和工作流材料。
- 每轮对话交互过程记录通常不在 Git 仓库中，需要从使用的智能体平台导出或截图归档。

### 10.4 测试与实验材料

| 材料 | 位置 |
|---|---|
| 后端单元测试 | `backend/internal/**/**_test.go`, `backend/pkg/**/**_test.go` |
| 后端集成测试 | `backend/tests/*integration*_test.go`, `backend/tests/sprint3_*_test.go` |
| BGroup 验收测试 | `backend/tests/bgroup_integration_test.go` |
| JWT 测试 | `backend/tests/jwt_test.go`, `backend/pkg/jwtutil/jwt_test.go` |
| 响应契约测试 | `backend/tests/response_contract_test.go` |
| 安全测试 | `backend/tests/security_authz_test.go` |
| 模糊测试 | `backend/tests/security_fuzz_test.go` |
| 压力测试 | `backend/tests/stress_test.go`, `backend/tests/jmeter/` |
| 压测报告 | `docs/STRESS_TEST/` |
| 安全测试报告 | `docs/SECURITY_TEST.md` |
| 监控说明 | `docs/Prometheus_And_Grafna.md` |
| 前端 Vitest 测试 | `frontend/src/**/*.test.ts` |
| 前端测试配置 | `frontend/vitest.config.ts` |

### 10.5 运行与复现材料

| 材料 | 位置 |
|---|---|
| Docker Compose | `docker-compose.yaml` |
| Grafana Dashboard | `infra/grafana/provisioning/dashboards/` |
| Prometheus 配置 | `infra/prometheus/` |
| 后端 seed 脚本 | `backend/scripts/seed/` |
| JMeter 数据生成脚本 | `backend/scripts/gen_jmeter_data/` |
| 后端启动入口 | `backend/cmd/server/` |
| 前端启动入口 | `frontend/package.json` |

### 10.6 需要人工补充的材料

| 材料 | 当前状态 | 建议处理 |
|---|---|---|
| 每位成员个人心得 | 模板已提供 | 按真实成员姓名和经历补充 |
| 每轮对话交互记录 | 仓库中未完整保存 | 从智能体平台导出或截图 |
| 课程平台要求的实验日志 | 仓库中可能不完整 | 按课程要求补齐 |
| 实机演示截图或录屏 | 需现场补充 | 验收彩排时保存 |
| 最新测试命令输出 | 需最终执行后确认 | 提交前重新跑测试并保存摘要 |
| MCP Server 具体配置 | 需根据实际使用确认 | 若使用过 MCP，提交对应配置与说明 |

---

## 十一、最终复现路径

### 11.1 启动完整系统

```powershell
cd C:\Users\Lenovo\.openclaw\workspace\UaaD
docker compose up -d
docker compose ps
```

启动后端：

```powershell
cd C:\Users\Lenovo\.openclaw\workspace\UaaD\backend
go run ./cmd/server
```

另开终端写入种子数据：

```powershell
cd C:\Users\Lenovo\.openclaw\workspace\UaaD\backend
go run ./scripts/seed
```

启动前端：

```powershell
cd C:\Users\Lenovo\.openclaw\workspace\UaaD\frontend
pnpm dev
```

访问地址：

- 前端：`http://localhost:5173`
- 后端健康检查：`http://localhost:8080/api/v1/health`
- Prometheus：`http://localhost:9090`
- Grafana：`http://localhost:3000`
- Grafana 默认账号：`admin / admin`

### 11.2 演示账号

| 角色 | 手机号 | 密码 | 用途 |
|---|---|---|---|
| 普通用户 | `13800000001` | `test123456` | 报名、订单、通知、推荐 |
| 普通用户备用 | `13800000002` | `test123456` | 越权测试、并发测试备用 |
| 商户 | `13800000004` | `test123456` | 创建活动、预热活动、发布活动 |

### 11.3 最终建议测试命令

后端：

```powershell
cd C:\Users\Lenovo\.openclaw\workspace\UaaD\backend
go test ./... -count=1
go test -v -tags=integration -count=1 ./tests/
go test -v -tags=bgroup -count=1 ./tests/
go test -v -tags=stress -bench=. -benchtime=10s -count=1 ./tests/
```

前端：

```powershell
cd C:\Users\Lenovo\.openclaw\workspace\UaaD\frontend
pnpm lint
pnpm build
pnpm test
```

安全测试：

```powershell
cd C:\Users\Lenovo\.openclaw\workspace\UaaD\backend
go test -v -tags=bgroup -run '^TestSec|^TestFuzz' -count=1 ./tests/
```

压测：

```powershell
cd C:\Users\Lenovo\.openclaw\workspace\UaaD\backend\tests\jmeter
bash run-jmeter-report.sh
```

---

## 十二、Sprint 6 DoD

- 已完成最终功能成果总结。
- 已总结关键技术栈与智能技术实践。
- 已描述开发流程、团队分工、沟通机制和协作模式。
- 已提供每位成员个人心得模板。
- 已完成缺陷与反思。
- 已提出课程内容、组织形式和教学方法建议。
- 已覆盖压力测试、微服务架构、系统监控、弹性设计、安全性保障五个技术维度。
- 已汇总智能体配置、规则文档、Skills、OpenSpec、测试报告、压测报告、安全报告等配套材料。
- 已列出需要人工补充的真实个人心得、对话记录、截图、录屏和最新测试输出。

---

## 十三、最终结论

经过多轮迭代，UAAD 项目已经形成了一个具备完整业务闭环、并发抢票能力、推荐能力、监控能力、安全测试基础和实机演示路径的课程项目。项目不仅完成了功能开发，也覆盖了工程化实践中的运行、测试、监控、压测、安全和文档治理。

从最终效果看，本项目的核心价值不只是实现了活动报名系统，而是在有限时间内实践了一个较完整的软件工程过程：需求分析、系统设计、迭代开发、智能体辅助、测试验证、缺陷修复、性能观察、安全加固和团队复盘。后续如果继续推进，可以优先补强生产级安全、自动化部署、服务拆分、告警体系和端到端自动化测试，使系统从课程验收项目进一步接近真实工程系统。
